# Blue Current V23.0.3 — Authentication Button Fix

The screenshot showed that the browser was still serving V23.0.1. This build:

- Uses a new top-level folder name.
- Versions all active CSS and JavaScript assets as V23.0.3.
- Updates the diagnostics badge to V23.0.3.
- Updates the server health version to V23.0.3.
- Applies a definitive gold style to both Sign in and Sign out.
- Keeps demo-account selector buttons dark.
- Preserves no-store asset headers.

Stop the old Node server before starting this folder.
