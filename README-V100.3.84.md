# Blue Current V100.3.84 — Direct Call CTA

This focused update replaces both public “Schedule a private walkthrough” buttons with a direct telephone action:

`Call Keith · 848-309-0042`

Both buttons use `tel:+18483090042`, retain accessible names, and work directly from phones and tablets. The contact email and pilot inquiry form remain unchanged.

## Install

Run `INSTALL-V100.3.84.ps1` from the extracted update folder. The installer requires certified V100.3.83, validates the application, runs the complete certification chain, and confirms that the runtime database hash is unchanged.
