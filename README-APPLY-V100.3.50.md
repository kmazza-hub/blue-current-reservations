# Apply V100.3.50

Overlay this package, run `npm install`, `npm run check`, the V100.3.50 focused test, and `npm run certify:pilot`. Keep live database and environment files unstaged.

Local development continues with `npm run pilot:lan`. The hosted rehearsal command uses a disposable copy and requires an explicit source path.
