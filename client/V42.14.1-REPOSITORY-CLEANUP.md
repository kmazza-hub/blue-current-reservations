# Blue Current V42.14.1 — Repository Hygiene

This maintenance release removes verified inactive duplicate project copies while preserving the V42.14.0 runtime.

Removed:
- `Blue-Current-v32.2.3-auth-startup-order-hotfix/` — embedded V32 snapshot.
- Root `modules/` — six exact duplicates of active `client/js/modules/*` V42.14 files.
- Root `app-v15.1.3.js` — exact duplicate of `client/js/app-v15.1.3.js`.
- Root `startup-loader.js` — exact duplicate of `client/js/startup-loader.js`.
- `client/appState.js` — exact duplicate of `client/js/appState.js`; active page references the latter.
- `server/client/` — inactive duplicate employee portal page; server static root is project `client/`.
- `server/server/` — inactive nested legacy server copy; `npm start` executes `server/server.js`.

Active runtime files were not changed by this maintenance release.
