(function(){"use strict";
function createBlueCurrentRelationshipGraphCenterModule(eventBus){const root=document.getElementById("relationshipGraphCenter");if(!root||!window.BlueCurrentRelationshipGraphEngine)return null;const engine=new window.BlueCurrentRelationshipGraphEngine(eventBus),$=id=>document.getElementById(id),esc=v=>String(v??"").replace(/[&<>\"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"}[c]));
 function render(){const s=engine.summary();$("relTotal").textContent=s.total;$("relEntities").textContent=s.entities;$("relConfidence").textContent=s.highConfidence;$("relTypes").textContent=s.types;$("relList").innerHTML=engine.edges.length?engine.edges.map(e=>`<article><div><strong>${esc(e.source)} <span>→ ${esc(e.type)} →</span> ${esc(e.target)}</strong><small>${e.confidence}% confidence · ${esc(e.evidence)}</small></div><button data-remove="${esc(e.id)}">Remove</button></article>`).join(""):"<p>No relationships recorded.</p>";}
 $("relForm")?.addEventListener("submit",e=>{e.preventDefault();try{engine.add({source:$("relSource")?.value,target:$("relTarget")?.value,type:$("relType")?.value,confidence:$("relConfidenceInput")?.value,evidence:$("relEvidence")?.value});$("relMessage").textContent="Relationship added to the hospitality graph.";e.target.reset();render();}catch(err){$("relMessage").textContent=err.message;}});
 $("relList")?.addEventListener("click",e=>{const b=e.target.closest("button[data-remove]");if(!b)return;engine.remove(b.dataset.remove);render();});render();return{engine,render};}
window.createBlueCurrentRelationshipGraphCenterModule=createBlueCurrentRelationshipGraphCenterModule;})();
