(function(){"use strict";function createBlueCurrentProductionHealthCenterModule(eventBus,appState){const root=document.getElementById("productionHealthCenter");if(!root||!window.BlueCurrentProductionHealthEngine)return null;const e=new window.BlueCurrentProductionHealthEngine(eventBus,appState),$=id=>document.getElementById(id);function draw(x){$("phtScore").textContent=`${x.score||0}%`;$("phtStatus").textContent=x.status||"—";$("phtTrusted").textContent=x.trusted?"YES":"NO";$("phtViolations").textContent=(x.violations||[]).length;$("phtList").innerHTML=(x.controls||[]).map(c=>`<article><div><strong>${c.label}</strong><span>${c.detail}</span></div><b class="${c.pass?"pass":"block"}">${c.pass?"PASS":"BLOCK"}</b></article>`).join("");}async function refresh(){try{draw(await e.run());}catch(err){$("phtStatus").textContent=err.message;}}$("phtRefresh")?.addEventListener("click",refresh);refresh();return{engine:e,refresh};}window.createBlueCurrentProductionHealthCenterModule=createBlueCurrentProductionHealthCenterModule;})();
