(function(){"use strict";
function createBlueCurrentOperationalMemoryCenterModule(eventBus,appState){const root=document.getElementById("operationalMemoryCenter");if(!root||!window.BlueCurrentOperationalMemoryEngine)return null;const engine=new window.BlueCurrentOperationalMemoryEngine(eventBus,appState),$=id=>document.getElementById(id),esc=v=>String(v??"").replace(/[&<>\"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"}[c]));
 function render(){const h=engine.read(),t=engine.trend();$("memoryCount").textContent=h.length;$("memoryTrend").textContent=t.status;$("memoryScore").textContent=`${t.score}%`;$("memoryLatest").textContent=h[0]?new Date(h[0].createdAt).toLocaleTimeString():"—";$("memoryList").innerHTML=h.slice(0,8).map(x=>`<article><div><strong>${esc(x.label)}</strong><span>${esc(x.owner)} · ${new Date(x.createdAt).toLocaleString()}</span></div><small>Wait ${x.signals.guestWait}m · Kitchen ${x.signals.kitchenPressure}% · Labor ${x.signals.labor}%</small></article>`).join("")||"<p>No operating checkpoints captured yet.</p>";}
 $("memoryCapture")?.addEventListener("click",()=>{engine.capture($("memoryLabel")?.value||"Operating checkpoint",$("memoryOwner")?.value||"Manager");render();});render();return{engine,render};}
window.createBlueCurrentOperationalMemoryCenterModule=createBlueCurrentOperationalMemoryCenterModule;})();