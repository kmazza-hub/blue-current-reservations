(function(){"use strict";
function createBlueCurrentAipRuntimeLifecycleCenterModule(){const root=document.getElementById('aipRuntimeLifecycleCenter');if(!root||!window.BlueCurrentAipRuntimeLifecycleEngine)return null;const e=new window.BlueCurrentAipRuntimeLifecycleEngine(),$=id=>document.getElementById(id),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));let runId=null;
 function render(x){const r=x.run;runId=r?.id||runId;$('arlStatus').textContent=r?.status||'—';$('arlRun').textContent=r?.id||'—';$('arlCheckpoints').textContent=x.checkpointCount||r?.checkpointCount||0;$('arlIsolation').textContent=x.safety?.liveExecutionAllowed===false?'ISOLATED':'CHECK';$('arlList').innerHTML=(x.checkpoints||[]).slice(0,15).map(c=>`<article><strong>${esc(c.id)}</strong><span>${esc(c.status)} · step ${c.currentStep||0} · ${new Date(c.createdAt).toLocaleString()}</span></article>`).join('')||'<p>No lifecycle checkpoints yet.</p>';}
 async function load(){try{render(await e.load(runId));}catch(err){$('arlMessage').textContent=err.message;}}
 async function act(action){try{if(!runId){const x=await e.load();runId=x.run?.id||null;}if(!runId)throw new Error('Create a persistent agent run first.');const x=await e.act(runId,action);$('arlMessage').textContent=`${action} recorded · live state changed: ${x.run?.liveStateChanged?'YES':'NO'}`;await load();}catch(err){$('arlMessage').textContent=err.message;}}
 root.addEventListener('click',ev=>{const action=ev.target.closest('[data-runtime-action]')?.dataset.runtimeAction;if(action)act(action);});$('arlRefresh')?.addEventListener('click',load);load();return{engine:e,load,act};}
window.createBlueCurrentAipRuntimeLifecycleCenterModule=createBlueCurrentAipRuntimeLifecycleCenterModule;
})();
