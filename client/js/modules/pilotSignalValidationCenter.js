(function(){"use strict";function createBlueCurrentPilotSignalValidationCenterModule(eventBus,appState){const root=document.getElementById("pilotSignalValidationCenter");if(!root||!window.BlueCurrentPilotSignalValidationEngine)return null;const e=new window.BlueCurrentPilotSignalValidationEngine(eventBus,appState),$=id=>document.getElementById(id);function draw(x){$("psvScore").textContent=`${x.score||0}%`;$("psvStatus").textContent=x.status||"—";$("psvValidated").textContent=x.validatedSessions||0;$("psvActive").textContent=x.activeSessions||0;$("psvList").innerHTML=(x.locations||[]).map(r=>`<article><div><strong>${r.locationName||r.locationId}</strong><span>${r.controls.map(c=>`${c.label}: ${c.detail}`).join(" · ")}</span></div><b class="${r.status==="validated"?"pass":r.status==="watch"?"watch":"block"}">${r.score}%</b></article>`).join("");}async function refresh(){try{draw(await e.run());}catch(err){$("psvStatus").textContent=err.message;}}$("psvRefresh")?.addEventListener("click",refresh);refresh();return{engine:e,refresh};}window.createBlueCurrentPilotSignalValidationCenterModule=createBlueCurrentPilotSignalValidationCenterModule;})();