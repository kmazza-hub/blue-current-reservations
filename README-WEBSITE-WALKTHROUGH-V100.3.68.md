# Blue Current website walkthrough repair

Replaces every active LeadConnector booking URL with a first-party `walkthrough.html` experience.

The new page stays inside the Blue Current brand, explains the 30-minute conversation, validates every required field, prepares a structured request to `keith@bluecurrentco.com`, retains a recoverable local draft, and offers direct email as a fallback.

Run `INSTALL-WEBSITE-WALKTHROUGH-V100.3.68.ps1`, review the result locally, then commit and push the website files to publish through the existing website deployment.
