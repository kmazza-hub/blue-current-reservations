# Apply V100.3.46

Overlay this package, run `npm install`, `npm run check`, the V100.3.46 focused test, and `npm run certify:pilot`.

Keep live database files unstaged. Start with `npm run pilot:lan`, sign in on the iPad, and append `?pilotAcceptance=1` to the printed LAN URL.
