# V100.2.98 — Host → Service Party Identity Integrity

Repairs the first cross-domain lifecycle gap found during Phase B certification.

The unified seating commit now records party size and seated time on the selected Floor table, updates the existing table-trust owner, and explicitly publishes the complete Host → Service handoff before removing the waiting card. The existing Service identity key safely deduplicates the explicit event and table-observer recovery path.

This restores accurate Active Covers and removes load-order dependence from Service intake. No second queue, state store, observer, or workflow was introduced.
