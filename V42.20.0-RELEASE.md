# Blue Current V42.20.0 — Provider Connection Runtime

V42.18 adds server-side secret bindings that store environment-variable references only. V42.19 adds signed HMAC-SHA256 webhook ingress into the existing adapter/canonical event path. V42.20 adds provider connection readiness across authentication, adapter mapping, verified delivery, source checkpoints, promotion state, and live-evidence certification.

No provider credential values are persisted to the Blue Current browser or database.
