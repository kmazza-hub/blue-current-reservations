# Blue Current V100.3.97 — Responsive Interaction Integrity

This forward-only maintenance release fixes the iPhone first-use **Got it** action. The card now dismisses before its optional preference write, survives unavailable Safari storage, and accepts both touch-pointer and click activation.

The release also keeps the first-use control above competing layers, reserves mobile space for the fixed Quick Jobs dock, and applies standalone safe-area spacing at the iPhone status bar and Home indicator. The responsive audit covers all ten operator workspaces, the 420–1366 px breakpoint contract, touch targets, installed-app assets, authentication, and the isolated runtime.

No restaurant records, readiness gates, provider write-back settings, authentication policy, or operational workflows are changed. The restaurant remains on its existing controlled `HOLD`.
