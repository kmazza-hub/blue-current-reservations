--- a/package.json
+++ b/package.json
@@ -1,8 +1,8 @@
 {
   "name": "blue-current-cloud",
-  "version": "46.25.0",
+  "version": "46.30.0",
   "private": true,
-  "description": "Blue Current Hospitality OS \u2014 reversible deprecation manifest, task evidence, and navigation cleanup",
+  "description": "Blue Current Hospitality OS \u2014 multi-day retirement certification and owner sign-off",
   "scripts": {
     "start": "node server/server.js",
     "check": "node scripts/validate.js"
--- a/server/server.js
+++ b/server/server.js
@@ -152,7 +152,7 @@
 });
 
 authService.initializePasswords().then(() => server.listen(PORT, () => {
-  console.log(`Blue Current Cloud V46.25.0 running at http://localhost:${PORT}`);
+  console.log(`Blue Current Cloud V46.30.0 running at http://localhost:${PORT}`);
   console.log(`Database: ${DB_PATH}`);
 })).catch(error => {
   console.error(error);
--- a/server/api/router.js
+++ b/server/api/router.js
@@ -74,7 +74,7 @@
     if (url.pathname === "/api/health" && request.method === "GET") {
       return sendJson(response, 200, {
         ok: true,
-        version: "46.25.0",
+        version: "46.30.0",
         database: "connected",
         auth: "enabled",
         realtimeClients: realtimeHub.count(),
--- a/client/index.html
+++ b/client/index.html
@@ -1,7 +1,7 @@
 <!doctype html>
 <html lang="en">
 <head>
-  <meta name="blue-current-build" content="46.25.0">
+  <meta name="blue-current-build" content="46.30.0">
   <meta charset="utf-8">
   <meta name="viewport" content="width=device-width, initial-scale=1">
   <title>Blue Current | Hospitality That Answers</title>
@@ -4162,6 +4162,7 @@
 <section class="v432-center v438-center v4316-center" id="operatorConsolidationCenter"><header><div><small>V46.11–V46.15 · Operator Consolidation + Usage Evidence</small><strong>Merge duplicate operator entry points into primary surfaces, apply role defaults, and collect evidence before deprecation.</strong><span>This wave changes navigation and presentation only. It records non-destructive operator-use evidence in AppState; no working module is deleted.</span></div><div class="v4316-actions"><button id="ocRefresh" type="button">Refresh</button><button id="ocApplyRole" type="button">Apply role defaults</button><button id="ocFullPlatform" type="button">Full platform</button></div></header><div class="v432-kpis"><article><small>Role</small><strong id="ocRole">MANAGER</strong></article><article><small>Default surfaces</small><strong id="ocDefaults">0</strong></article><article><small>Merged primary</small><strong id="ocMerged">0</strong></article><article><small>Use evidence</small><strong id="ocUseEvidence">0</strong></article><article><small>Deprecation evidence</small><strong id="ocDepReady">0</strong></article></div><div class="v4316-answer" id="ocHeadline">Preparing consolidated operator navigation…</div><div id="ocPrimary" class="v432-list"></div><div id="ocDeprecation" class="v432-list"></div></section>
 <section class="v432-center v438-center v4316-center" id="operatorConsolidationScorecardCenter"><header><div><small>V46.16–V46.20 · Consolidation Scorecard</small><strong>Measure navigation duplication, workflow coverage, handoff pressure, role usage, and evidence-backed deprecation readiness.</strong><span>The scorecard produces a review shortlist only. It cannot remove modules automatically.</span></div><button id="ocsRefresh" type="button">Refresh scorecard</button></header><div class="v432-kpis"><article><small>Score</small><strong id="ocsScore">0%</strong></article><article><small>Grade</small><strong id="ocsGrade">—</strong></article><article><small>Duplicate nav</small><strong id="ocsDupNav">0</strong></article><article><small>Workflow coverage</small><strong id="ocsCoverage">0%</strong></article><article><small>Attention handoffs</small><strong id="ocsHandoffs">0</strong></article><article><small>Use evidence</small><strong id="ocsEvidence">0</strong></article><article><small>Review shortlist</small><strong id="ocsShortlist">0</strong></article></div><div class="v4316-answer" id="ocsHeadline">Calculating operator consolidation score…</div><div id="ocsNav" class="v432-list"></div><div id="ocsRoles" class="v432-list"></div><div id="ocsDeprecation" class="v432-list"></div></section>
 <section class="v432-center v438-center v4316-center" id="deprecationManifestCenter"><header><div><small>V46.21–V46.25 · Human-Approved Deprecation Preview</small><strong>Use evidence thresholds to create a review manifest, require human approval, then hide candidates reversibly behind a feature flag.</strong><span>Approved preview hiding is not code deletion. Full capability remains present and can be restored immediately.</span></div><button id="odmRefresh" type="button">Refresh</button></header><div class="v4316-actions"><button id="odmDraft" type="button">Build draft manifest</button><input id="odmApprover" class="v4312-input" placeholder="Approver name"><button id="odmApprove" type="button">Approve manifest</button><button id="odmEnable" type="button">Enable hide preview</button><button id="odmDisable" type="button">Disable preview</button></div><div class="v432-kpis"><article><small>Status</small><strong id="odmStatus">draft</strong></article><article><small>Items</small><strong id="odmItems">0</strong></article><article><small>Approved</small><strong id="odmApproved">0</strong></article><article><small>Hidden preview</small><strong id="odmHidden">0</strong></article><article><small>Feature flag</small><strong id="odmFlag">OFF</strong></article></div><div class="v4316-answer" id="odmHeadline">Build a draft manifest from evidence-backed candidates.</div><p id="odmThresholds"></p><div id="odmList" class="v432-list"></div></section>
+<section class="v432-center v438-center v4316-center" id="retirementCertificationCenter"><header><div><small>V46.26–V46.30 · Retirement Certification</small><strong>Require multi-day preview evidence, task-quality thresholds, manifest revalidation, and owner sign-off before a surface can become a retirement candidate.</strong><span>Certification does not delete code. A certified candidate remains reversible and requires a later explicit deletion decision.</span></div><button id="orcRefresh" type="button">Refresh</button></header><div class="v4316-actions"><select id="orcSurface"></select><input id="orcOwner" class="v4312-input" placeholder="Surface owner / certifier"><input id="orcNote" class="v4312-input" placeholder="Observation or sign-off note"><button id="orcObserve" type="button">Record preview observation</button><button id="orcSignoff" type="button">Owner sign-off</button><button id="orcRevalidate" type="button">Revalidate manifest</button><button id="orcCertify" type="button">Certify candidate</button></div><div class="v432-kpis"><article><small>Eligible candidates</small><strong id="orcEligible">0</strong></article><article><small>Certified</small><strong id="orcCertified">0</strong></article><article><small>Task success</small><strong id="orcTaskSuccess">100%</strong></article><article><small>Avg resolution</small><strong id="orcResolution">—</strong></article></div><div class="v4316-answer" id="orcHeadline">Retirement certification requires an approved, revalidated hide preview.</div><div id="orcList" class="v432-list"></div></section>
 <section class="guided-shift-center" id="guidedShiftCenter" aria-labelledby="guidedShiftTitle">
             <header class="guided-shift-header">
               <div><small>V36.1 · Guided Operator Flow</small><strong id="guidedShiftTitle">Stay ahead of the shift without hunting through dashboards.</strong><span id="guidedShiftUpdated">Building operator flow…</span></div>
@@ -8627,6 +8628,8 @@
 <script type="text/bluecurrent-deferred" data-pack="operations" data-pack-explicit="true" data-center="operatorConsolidationScorecardCenter" data-src="js/modules/operatorConsolidationScorecardCenter.js?v=46.20.0"></script>
 <script type="text/bluecurrent-deferred" data-pack="operations" data-pack-explicit="true" data-center="deprecationManifestCenter" data-src="js/modules/deprecationManifestEngine.js?v=46.25.0"></script>
 <script type="text/bluecurrent-deferred" data-pack="operations" data-pack-explicit="true" data-center="deprecationManifestCenter" data-src="js/modules/deprecationManifestCenter.js?v=46.25.0"></script>
-<script src="js/startup-loader.js?v=46.25.0"></script>
+<script type="text/bluecurrent-deferred" data-pack="operations" data-pack-explicit="true" data-center="retirementCertificationCenter" data-src="js/modules/retirementCertificationEngine.js?v=46.30.0"></script>
+<script type="text/bluecurrent-deferred" data-pack="operations" data-pack-explicit="true" data-center="retirementCertificationCenter" data-src="js/modules/retirementCertificationCenter.js?v=46.30.0"></script>
+<script src="js/startup-loader.js?v=46.30.0"></script>
 </body>
 </html>
--- a/client/js/app-v15.1.3.js
+++ b/client/js/app-v15.1.3.js
@@ -1214,6 +1214,7 @@
 const operatorConsolidationCenterModule = startupRegistry.register("operatorConsolidationCenter", shouldInitializeCenter("operatorConsolidationCenter") ? window.createBlueCurrentOperatorConsolidationCenterModule?.(eventBus, appState) : null, ["eventBus", "appState", "operatorSurfaceRationalizationCenter", "roleExperienceCenter"]);
 const operatorConsolidationScorecardCenterModule = startupRegistry.register("operatorConsolidationScorecardCenter", shouldInitializeCenter("operatorConsolidationScorecardCenter") ? window.createBlueCurrentOperatorConsolidationScorecardCenterModule?.(eventBus, appState) : null, ["eventBus", "appState", "operatorConsolidationCenter"]);
 const deprecationManifestCenterModule = startupRegistry.register("deprecationManifestCenter", shouldInitializeCenter("deprecationManifestCenter") ? window.createBlueCurrentDeprecationManifestCenterModule?.(eventBus, appState) : null, ["eventBus", "appState", "operatorConsolidationScorecardCenter"]);
+const retirementCertificationCenterModule = startupRegistry.register("retirementCertificationCenter", shouldInitializeCenter("retirementCertificationCenter") ? window.createBlueCurrentRetirementCertificationCenterModule?.(eventBus, appState) : null, ["eventBus", "appState", "deprecationManifestCenter"]);
 
 const operatorCopilotCenterModule = startupRegistry.register(
   "operatorCopilotCenter",
--- a/client/js/startup-loader.js
+++ b/client/js/startup-loader.js
@@ -209,7 +209,7 @@
       }));
       const summary = document.getElementById("startupDiagnosticsSummary");
       const dot = document.getElementById("startupDiagnosticsDot");
-      if (summary) summary.textContent = `V46.25.0 ready · ${duration}ms`;
+      if (summary) summary.textContent = `V46.30.0 ready · ${duration}ms`;
       if (dot) dot.className = "ok";
       // Local development can be held open by optional third-party assets. Once the
       // application is ready, stop those nonessential pending resource loads.
--- a/client/js/modules/operatorConsolidationEngine.js
+++ b/client/js/modules/operatorConsolidationEngine.js
@@ -20,12 +20,14 @@
   evidence(){const s=this.state(),items=Array.isArray(s.operatorUseEvidence)?s.operatorUseEvidence:[];return items.slice(0,500);}
   recordUse(surfaceId,action="open",metadata={}){if(!surfaceId)return null;const s=this.state(),items=Array.isArray(s.operatorUseEvidence)?s.operatorUseEvidence:[],entry={id:`OUE-${Date.now().toString(36).toUpperCase()}`,surfaceId,action,role:this.role(),at:new Date().toISOString(),metadata};const next=[entry,...items].slice(0,500);this.appState?.update?.({operatorUseEvidence:next});this.eventBus?.emit?.("operator-use-evidence:recorded",structuredClone(entry));return entry;}
   resolvePrimarySurface(surfaceId){return this.mergeMap[surfaceId]||surfaceId;}
-  recordTaskCompletion(surfaceId,taskId,outcome="completed",metadata={}){const primary=this.resolvePrimarySurface(surfaceId),entry=this.recordUse(primary,"task-completed",{taskId:String(taskId||"task"),outcome,sourceSurface:surfaceId,...metadata});this.eventBus?.emit?.("operator-task-completion:recorded",{surfaceId:primary,sourceSurface:surfaceId,taskId,outcome});return entry;}
-  evidenceThresholds(){return {minimumObservedDays:3,minimumOpenEvents:3,minimumTaskCompletions:1,minimumDistinctRoles:1,minimumPrimaryWorkflowCoverage:100,automaticDeletion:false,version:"46.25.0"};}
+  recordTaskStart(surfaceId,taskId,metadata={}){const primary=this.resolvePrimarySurface(surfaceId);return this.recordUse(primary,"task-started",{taskId:String(taskId||"task"),sourceSurface:surfaceId,...metadata});}
+  recordTaskCompletion(surfaceId,taskId,outcome="completed",metadata={}){const primary=this.resolvePrimarySurface(surfaceId),task=String(taskId||"task"),prior=this.evidence().find(x=>x.surfaceId===primary&&x.action==="task-started"&&x.metadata?.taskId===task),completedAt=new Date(),startedAt=metadata.startedAt||prior?.at||null,durationMs=startedAt?Math.max(0,completedAt.getTime()-new Date(startedAt).getTime()):null,entry=this.recordUse(primary,"task-completed",{taskId:task,outcome,sourceSurface:surfaceId,startedAt,durationMs,...metadata});this.eventBus?.emit?.("operator-task-completion:recorded",{surfaceId:primary,sourceSurface:surfaceId,taskId:task,outcome,durationMs});return entry;}
+  taskMetrics(){const events=this.evidence(),roles={};for(const e of events.filter(x=>x.action==="task-completed")){const role=e.role||"unknown";roles[role] ||= {role,total:0,succeeded:0,failed:0,durations:[]};roles[role].total++;const outcome=String(e.metadata?.outcome||"completed").toLowerCase();if(["completed","success","resolved","approved"].includes(outcome))roles[role].succeeded++;else roles[role].failed++;if(Number.isFinite(Number(e.metadata?.durationMs)))roles[role].durations.push(Number(e.metadata.durationMs));}return Object.values(roles).map(x=>({...x,successRate:x.total?Math.round(100*x.succeeded/x.total):0,averageResolutionMs:x.durations.length?Math.round(x.durations.reduce((a,b)=>a+b,0)/x.durations.length):null}));}
+  evidenceThresholds(){return {minimumObservedDays:3,minimumOpenEvents:3,minimumTaskCompletions:1,minimumDistinctRoles:1,minimumPrimaryWorkflowCoverage:100,minimumPreviewSnapshots:3,minimumTaskSuccessRate:95,maximumAverageResolutionMinutes:15,manifestValidityHours:72,automaticDeletion:false,version:"46.30.0"};}
   evidenceSummary(ids=null){const evidence=this.evidence(),scope=Array.isArray(ids)?ids:[...new Set(evidence.map(x=>x.surfaceId))];return scope.map(id=>{const hits=evidence.filter(x=>x.surfaceId===id),roles=[...new Set(hits.map(x=>x.role))];return {surfaceId:id,uses:hits.length,lastUsedAt:hits[0]?.at||null,roles,hasEvidence:hits.length>0};}).sort((a,b)=>b.uses-a.uses);}
   deprecationEvidence(ids=[]){const audit=this.rationalizationEngine?.snapshot?.(ids)||{items:ids.map(id=>({id,disposition:"supporting"}))};const usage=new Map(this.evidenceSummary(ids).map(x=>[x.surfaceId,x]));return audit.items.filter(x=>x.disposition==="deprecation-candidate").map(item=>{const u=usage.get(item.id)||{uses:0,lastUsedAt:null,roles:[]};return {...item,usage:u,recommendation:u.uses===0?"evidence-needed":u.uses<=1?"retain-until-more-evidence":"retain-active-use"};});}
-  navigationModel(role=this.role()){const defaults=this.defaultSurfaces(role);return {role,defaultSurfaces:defaults,count:defaults.length,duplicateEntryPolicy:"merge-entry-points-into-primary-targets",fallback:"full-platform",version:"46.25.0"};}
-  snapshot(){const rational=this.rationalizationEngine?.snapshot?.()||null,role=this.role(),defaults=this.defaultSurfaces(role),primary=this.consolidatedPrimary(),evidence=this.evidence(),deprecation=rational?this.deprecationEvidence(rational.items.map(x=>x.id)):[];return {capturedAt:new Date().toISOString(),role,navigation:this.navigationModel(role),primary,mergedPrimaryCount:primary.filter(x=>x.summary.sources.length>0).length,evidenceCount:evidence.length,evidenceSummary:this.evidenceSummary(),deprecationCandidates:deprecation,deprecationReady:deprecation.filter(x=>x.recommendation!=="evidence-needed").length,policy:"merge-before-delete-and-require-use-evidence",thresholds:this.evidenceThresholds(),version:"46.25.0"};}
+  navigationModel(role=this.role()){const defaults=this.defaultSurfaces(role);return {role,defaultSurfaces:defaults,count:defaults.length,duplicateEntryPolicy:"merge-entry-points-into-primary-targets",fallback:"full-platform",version:"46.30.0"};}
+  snapshot(){const rational=this.rationalizationEngine?.snapshot?.()||null,role=this.role(),defaults=this.defaultSurfaces(role),primary=this.consolidatedPrimary(),evidence=this.evidence(),deprecation=rational?this.deprecationEvidence(rational.items.map(x=>x.id)):[];return {capturedAt:new Date().toISOString(),role,navigation:this.navigationModel(role),primary,mergedPrimaryCount:primary.filter(x=>x.summary.sources.length>0).length,evidenceCount:evidence.length,evidenceSummary:this.evidenceSummary(),deprecationCandidates:deprecation,deprecationReady:deprecation.filter(x=>x.recommendation!=="evidence-needed").length,policy:"merge-before-delete-and-require-use-evidence",thresholds:this.evidenceThresholds(),version:"46.30.0"};}
 }
 if(typeof module!=="undefined"&&module.exports)module.exports=BlueCurrentOperatorConsolidationEngine;
 if(global)global.BlueCurrentOperatorConsolidationEngine=BlueCurrentOperatorConsolidationEngine;
--- a/client/js/modules/deprecationManifestEngine.js
+++ b/client/js/modules/deprecationManifestEngine.js
@@ -3,13 +3,14 @@
   constructor({eventBus,appState,consolidationEngine,rationalizationEngine,scorecardEngine}={}){this.eventBus=eventBus;this.appState=appState;this.consolidationEngine=consolidationEngine;this.rationalizationEngine=rationalizationEngine;this.scorecardEngine=scorecardEngine;}
   state(){try{return this.appState?.getState?.()||{};}catch{return {};}}
   thresholds(){return this.consolidationEngine?.evidenceThresholds?.()||{minimumObservedDays:3,minimumOpenEvents:3,minimumTaskCompletions:1,minimumDistinctRoles:1,minimumPrimaryWorkflowCoverage:100,automaticDeletion:false};}
-  manifest(){const s=this.state();return s.operatorDeprecationManifest||{version:"46.25.0",status:"draft",approvedBy:null,approvedAt:null,featureFlagEnabled:false,items:[]};}
+  manifest(){const s=this.state();return s.operatorDeprecationManifest||{version:"46.30.0",status:"draft",approvedBy:null,approvedAt:null,featureFlagEnabled:false,items:[]};}
   evidenceFor(surfaceId){const events=(this.consolidationEngine?.evidence?.()||[]).filter(x=>x.surfaceId===surfaceId),days=[...new Set(events.map(x=>String(x.at||"").slice(0,10)).filter(Boolean))],roles=[...new Set(events.map(x=>x.role).filter(Boolean))];return {surfaceId,events:events.length,opens:events.filter(x=>x.action==="open").length,taskCompletions:events.filter(x=>x.action==="task-completed").length,days:days.length,roles:roles.length,lastUsedAt:events[0]?.at||null};}
   candidate(surfaceId){const rational=this.rationalizationEngine?.classify?.(surfaceId)||{disposition:"supporting"},t=this.thresholds(),ev=this.evidenceFor(surfaceId),coverage=this.scorecardEngine?.workflowCoverage?.("manager")||{percent:0};const zeroUse=ev.events===0,coveragePass=coverage.percent>=t.minimumPrimaryWorkflowCoverage;return {surfaceId,disposition:rational.disposition,evidence:ev,coverage:coverage.percent,eligibleForManifest:rational.disposition==="deprecation-candidate"&&zeroUse&&coveragePass,reason:rational.disposition!=="deprecation-candidate"?"Not a deprecation candidate":!zeroUse?"Observed use exists":!coveragePass?"Primary workflow coverage insufficient":"Zero observed use with full primary workflow coverage",automaticDeletion:false};}
-  createDraft(surfaceIds=[]){const candidates=surfaceIds.map(id=>this.candidate(id)),items=candidates.filter(x=>x.eligibleForManifest).map(x=>({surfaceId:x.surfaceId,status:"proposed-hide",reason:x.reason,featureFlag:false,approved:false,automaticDeletion:false}));const manifest={version:"46.25.0",status:"draft",createdAt:new Date().toISOString(),approvedBy:null,approvedAt:null,featureFlagEnabled:false,items,candidates,policy:"human-approved-reversible-hide-before-delete",automaticDeletion:false};this.appState?.update?.({operatorDeprecationManifest:manifest});this.eventBus?.emit?.("operator-deprecation-manifest:updated",structuredClone(manifest));return manifest;}
-  approve(approver){const current=this.manifest();if(!current.items?.length)throw new Error("Manifest has no eligible items.");const next={...current,status:"approved",approvedBy:String(approver||"Operator"),approvedAt:new Date().toISOString(),items:current.items.map(x=>({...x,approved:true}))};this.appState?.update?.({operatorDeprecationManifest:next});this.eventBus?.emit?.("operator-deprecation-manifest:updated",structuredClone(next));return next;}
-  setFeatureFlag(enabled){const current=this.manifest();if(enabled&&current.status!=="approved")throw new Error("Human approval is required before enabling deprecation hiding.");const next={...current,featureFlagEnabled:Boolean(enabled),items:(current.items||[]).map(x=>({...x,featureFlag:Boolean(enabled)&&x.approved===true}))};this.appState?.update?.({operatorDeprecationManifest:next});if(typeof document!=="undefined"){for(const item of next.items){const el=document.getElementById(item.surfaceId);if(!el)continue;const hide=next.featureFlagEnabled&&item.featureFlag;if(hide){el.hidden=true;el.setAttribute("aria-hidden","true");el.setAttribute("inert","");}else{el.hidden=false;el.removeAttribute("aria-hidden");el.removeAttribute("inert");}}document.documentElement.dataset.deprecationPreview=next.featureFlagEnabled?"true":"false";}this.eventBus?.emit?.("operator-deprecation-manifest:flag-changed",{enabled:next.featureFlagEnabled});return next;}
-  snapshot(){const manifest=this.manifest(),t=this.thresholds();return {manifest,thresholds:t,approvedItems:(manifest.items||[]).filter(x=>x.approved).length,hiddenPreviewItems:(manifest.items||[]).filter(x=>x.featureFlag).length,policy:"human-approved-reversible-hide-before-delete",automaticDeletion:false,version:"46.25.0"};}
+  createDraft(surfaceIds=[]){const candidates=surfaceIds.map(id=>this.candidate(id)),items=candidates.filter(x=>x.eligibleForManifest).map(x=>({surfaceId:x.surfaceId,status:"proposed-hide",reason:x.reason,featureFlag:false,approved:false,ownerSignoff:null,automaticDeletion:false})),now=Date.now(),validHours=this.thresholds().manifestValidityHours||72;const manifest={version:"46.30.0",status:"draft",createdAt:new Date(now).toISOString(),expiresAt:new Date(now+validHours*3600000).toISOString(),approvedBy:null,approvedAt:null,lastRevalidatedAt:null,revalidationStatus:"pending",featureFlagEnabled:false,items,candidates,policy:"human-approved-reversible-hide-before-delete",automaticDeletion:false};this.appState?.update?.({operatorDeprecationManifest:manifest});this.eventBus?.emit?.("operator-deprecation-manifest:updated",structuredClone(manifest));return manifest;}
+  approve(approver){const current=this.manifest();if(!current.items?.length)throw new Error("Manifest has no eligible items.");if(current.expiresAt&&new Date(current.expiresAt).getTime()<=Date.now())throw new Error("Manifest expired; rebuild before approval.");const next={...current,status:"approved",approvedBy:String(approver||"Operator"),approvedAt:new Date().toISOString(),items:current.items.map(x=>({...x,approved:true}))};this.appState?.update?.({operatorDeprecationManifest:next});this.eventBus?.emit?.("operator-deprecation-manifest:updated",structuredClone(next));return next;}
+  revalidate(){const current=this.manifest(),expired=Boolean(current.expiresAt)&&new Date(current.expiresAt).getTime()<=Date.now(),coverage=this.scorecardEngine?.workflowCoverage?.("manager")||{percent:0},items=(current.items||[]).map(item=>{const c=this.candidate(item.surfaceId);return {...item,revalidationEligible:c.eligibleForManifest,revalidationReason:c.reason};}),valid=!expired&&coverage.percent>=this.thresholds().minimumPrimaryWorkflowCoverage&&items.every(x=>x.revalidationEligible);const next={...current,lastRevalidatedAt:new Date().toISOString(),revalidationStatus:valid?"valid":"invalid",status:valid&&current.status==="approved"?"approved":current.status,items};this.appState?.update?.({operatorDeprecationManifest:next});this.eventBus?.emit?.("operator-deprecation-manifest:updated",structuredClone(next));return {manifest:next,valid,expired,coverage:coverage.percent};}
+  setFeatureFlag(enabled){const current=this.manifest();if(enabled&&current.status!=="approved")throw new Error("Human approval is required before enabling deprecation hiding.");if(enabled&&current.revalidationStatus!=="valid")throw new Error("Manifest must pass revalidation before enabling deprecation hiding.");const next={...current,featureFlagEnabled:Boolean(enabled),previewStartedAt:enabled&&!current.previewStartedAt?new Date().toISOString():current.previewStartedAt||null,items:(current.items||[]).map(x=>({...x,featureFlag:Boolean(enabled)&&x.approved===true}))};this.appState?.update?.({operatorDeprecationManifest:next});if(typeof document!=="undefined"){for(const item of next.items){const el=document.getElementById(item.surfaceId);if(!el)continue;const hide=next.featureFlagEnabled&&item.featureFlag;if(hide){el.hidden=true;el.setAttribute("aria-hidden","true");el.setAttribute("inert","");}else{el.hidden=false;el.removeAttribute("aria-hidden");el.removeAttribute("inert");}}document.documentElement.dataset.deprecationPreview=next.featureFlagEnabled?"true":"false";}this.eventBus?.emit?.("operator-deprecation-manifest:flag-changed",{enabled:next.featureFlagEnabled});return next;}
+  snapshot(){const manifest=this.manifest(),t=this.thresholds(),expired=Boolean(manifest.expiresAt)&&new Date(manifest.expiresAt).getTime()<=Date.now();return {manifest,thresholds:t,expired,approvedItems:(manifest.items||[]).filter(x=>x.approved).length,hiddenPreviewItems:(manifest.items||[]).filter(x=>x.featureFlag).length,policy:"human-approved-reversible-hide-before-delete",automaticDeletion:false,version:"46.30.0"};}
 }
 if(typeof module!=="undefined"&&module.exports)module.exports=BlueCurrentDeprecationManifestEngine;
 if(global)global.BlueCurrentDeprecationManifestEngine=BlueCurrentDeprecationManifestEngine;
--- a/client/js/modules/retirementCertificationEngine.js
+++ b/client/js/modules/retirementCertificationEngine.js
@@ -0,0 +1,28 @@
+(function(global){"use strict";
+class BlueCurrentRetirementCertificationEngine{
+  constructor({eventBus,appState,consolidationEngine,manifestEngine,scorecardEngine}={}){this.eventBus=eventBus;this.appState=appState;this.consolidationEngine=consolidationEngine;this.manifestEngine=manifestEngine;this.scorecardEngine=scorecardEngine;}
+  state(){try{return this.appState?.getState?.()||{};}catch{return {};}}
+  thresholds(){return this.consolidationEngine?.evidenceThresholds?.()||{};}
+  observations(){const s=this.state();return Array.isArray(s.operatorRetirementPreviewEvidence)?s.operatorRetirementPreviewEvidence:[];}
+  recordObservation(surfaceId,{workflowCoverage=null,taskFailures=0,regressions=0,note="",at=null}={}){const manifest=this.manifestEngine?.manifest?.()||{},item=(manifest.items||[]).find(x=>x.surfaceId===surfaceId);if(!item||!item.featureFlag)throw new Error("Surface must be in an enabled hide preview before observation.");const coverage=workflowCoverage??this.scorecardEngine?.workflowCoverage?.("manager")?.percent??0,entry={id:`ORP-${Date.now().toString(36).toUpperCase()}`,surfaceId,at:at||new Date().toISOString(),workflowCoverage:Number(coverage),taskFailures:Number(taskFailures||0),regressions:Number(regressions||0),note:String(note||"").slice(0,500),previewHidden:true};const next=[entry,...this.observations()].slice(0,500);this.appState?.update?.({operatorRetirementPreviewEvidence:next});this.eventBus?.emit?.("operator-retirement-preview:observed",structuredClone(entry));return entry;}
+  ownerSignoff(surfaceId,owner,decision="approve",note=""){const manifest=this.manifestEngine?.manifest?.()||{},items=(manifest.items||[]).map(x=>x.surfaceId===surfaceId?{...x,ownerSignoff:{owner:String(owner||"Owner"),decision:String(decision),note:String(note||"").slice(0,500),at:new Date().toISOString()}}:x);if(!items.some(x=>x.surfaceId===surfaceId))throw new Error("Surface is not in the manifest.");const next={...manifest,items};this.appState?.update?.({operatorDeprecationManifest:next});this.eventBus?.emit?.("operator-deprecation-manifest:updated",structuredClone(next));return next.items.find(x=>x.surfaceId===surfaceId).ownerSignoff;}
+  taskQuality(){const metrics=this.consolidationEngine?.taskMetrics?.()||[];const total=metrics.reduce((s,x)=>s+x.total,0),succeeded=metrics.reduce((s,x)=>s+x.succeeded,0),failed=metrics.reduce((s,x)=>s+x.failed,0),durations=metrics.filter(x=>Number.isFinite(x.averageResolutionMs)&&x.total>0);const avg=durations.length?Math.round(durations.reduce((s,x)=>s+x.averageResolutionMs*x.total,0)/durations.reduce((s,x)=>s+x.total,0)):null;return {byRole:metrics,total,succeeded,failed,successRate:total?Math.round(100*succeeded/total):100,averageResolutionMs:avg,averageResolutionMinutes:avg==null?null:Math.round(avg/600)/100};}
+  surfaceCertification(surfaceId){const t=this.thresholds(),manifest=this.manifestEngine?.manifest?.()||{},item=(manifest.items||[]).find(x=>x.surfaceId===surfaceId),obs=this.observations().filter(x=>x.surfaceId===surfaceId),days=[...new Set(obs.map(x=>String(x.at).slice(0,10)))],quality=this.taskQuality(),coverage=this.scorecardEngine?.workflowCoverage?.("manager")||{percent:0},signoff=item?.ownerSignoff||null,checks=[
+    {id:"manifest-approved",pass:manifest.status==="approved"&&item?.approved===true,detail:manifest.status||"no manifest"},
+    {id:"manifest-valid",pass:manifest.revalidationStatus==="valid"&&(!manifest.expiresAt||new Date(manifest.expiresAt).getTime()>Date.now()),detail:manifest.revalidationStatus||"not revalidated"},
+    {id:"preview-enabled",pass:item?.featureFlag===true,detail:item?.featureFlag?"hidden preview active":"preview not active"},
+    {id:"observation-days",pass:days.length>=Number(t.minimumObservedDays||3),detail:`${days.length}/${t.minimumObservedDays||3} observed day(s)`},
+    {id:"preview-snapshots",pass:obs.length>=Number(t.minimumPreviewSnapshots||3),detail:`${obs.length}/${t.minimumPreviewSnapshots||3} preview observation(s)`},
+    {id:"workflow-coverage",pass:coverage.percent>=Number(t.minimumPrimaryWorkflowCoverage||100)&&obs.every(x=>x.workflowCoverage>=Number(t.minimumPrimaryWorkflowCoverage||100)),detail:`${coverage.percent}% current coverage`},
+    {id:"regressions",pass:obs.every(x=>x.regressions===0&&x.taskFailures===0),detail:`${obs.reduce((s,x)=>s+x.regressions+x.taskFailures,0)} preview failure/regression signal(s)`},
+    {id:"task-success",pass:quality.successRate>=Number(t.minimumTaskSuccessRate||95),detail:`${quality.successRate}% task success`},
+    {id:"time-to-resolution",pass:quality.averageResolutionMinutes==null||quality.averageResolutionMinutes<=Number(t.maximumAverageResolutionMinutes||15),detail:quality.averageResolutionMinutes==null?"No duration evidence yet":`${quality.averageResolutionMinutes} min avg resolution`},
+    {id:"owner-signoff",pass:signoff?.decision==="approve",detail:signoff?`${signoff.owner} · ${signoff.decision}`:"Owner sign-off missing"}
+  ];const eligible=checks.every(x=>x.pass);return {surfaceId,eligible,status:eligible?"retirement-candidate":"preview-incomplete",checks,observedDays:days.length,observations:obs.length,taskQuality:quality,ownerSignoff:signoff,automaticDeletion:false};}
+  candidateSet(){const manifest=this.manifestEngine?.manifest?.()||{},items=(manifest.items||[]).map(x=>this.surfaceCertification(x.surfaceId));return {items,eligible:items.filter(x=>x.eligible),eligibleCount:items.filter(x=>x.eligible).length,policy:"certify-retirement-never-auto-delete",automaticDeletion:false,version:"46.30.0"};}
+  certify(surfaceId,certifier){const result=this.surfaceCertification(surfaceId);if(!result.eligible)throw new Error("Surface has not satisfied retirement certification.");const s=this.state(),certs=Array.isArray(s.operatorRetirementCertifications)?s.operatorRetirementCertifications:[],record={id:`ORC-${Date.now().toString(36).toUpperCase()}`,surfaceId,status:"retirement-candidate-certified",certifiedBy:String(certifier||"Operator"),certifiedAt:new Date().toISOString(),checks:result.checks,automaticDeletion:false,codeDeletionAllowed:false};const next=[record,...certs.filter(x=>x.surfaceId!==surfaceId)].slice(0,200);this.appState?.update?.({operatorRetirementCertifications:next});this.eventBus?.emit?.("operator-retirement-candidate:certified",structuredClone(record));return record;}
+  snapshot(){const set=this.candidateSet(),s=this.state(),certs=Array.isArray(s.operatorRetirementCertifications)?s.operatorRetirementCertifications:[];return {candidateSet:set,certifications:certs,certifiedCount:certs.filter(x=>x.status==="retirement-candidate-certified").length,taskQuality:this.taskQuality(),policy:"certify-retirement-never-auto-delete",automaticDeletion:false,codeDeletionAllowed:false,version:"46.30.0"};}
+}
+if(typeof module!=="undefined"&&module.exports)module.exports=BlueCurrentRetirementCertificationEngine;
+if(global)global.BlueCurrentRetirementCertificationEngine=BlueCurrentRetirementCertificationEngine;
+})(typeof window!=="undefined"?window:globalThis);--- a/client/js/modules/retirementCertificationCenter.js
+++ b/client/js/modules/retirementCertificationCenter.js
@@ -0,0 +1,8 @@
+(function(){"use strict";
+function createBlueCurrentRetirementCertificationCenterModule(eventBus,appState){const root=document.getElementById("retirementCertificationCenter");if(!root||!window.BlueCurrentRetirementCertificationEngine)return null;const rational=new window.BlueCurrentOperatorSurfaceRationalizationEngine({eventBus,appState}),consolidation=new window.BlueCurrentOperatorConsolidationEngine({eventBus,appState,rationalizationEngine:rational}),flow=new window.BlueCurrentOperatorServiceFlowEngine({eventBus,appState}),score=new window.BlueCurrentOperatorConsolidationScorecardEngine({eventBus,appState,consolidationEngine:consolidation,rationalizationEngine:rational,flowEngine:flow}),manifest=new window.BlueCurrentDeprecationManifestEngine({eventBus,appState,consolidationEngine:consolidation,rationalizationEngine:rational,scorecardEngine:score}),e=new window.BlueCurrentRetirementCertificationEngine({eventBus,appState,consolidationEngine:consolidation,manifestEngine:manifest,scorecardEngine:score}),$=id=>document.getElementById(id),esc=v=>String(v??"").replace(/[&<>\"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'\"':"&quot;","'":"&#39;"}[c]));
+function selected(){return $("orcSurface").value;}
+function refreshOptions(){const items=manifest.manifest().items||[],current=$("orcSurface").value;$("orcSurface").innerHTML=items.map(x=>`<option value="${esc(x.surfaceId)}">${esc(x.surfaceId)}</option>`).join("");if(items.some(x=>x.surfaceId===current))$("orcSurface").value=current;}
+function render(s){refreshOptions();$("orcEligible").textContent=s.candidateSet.eligibleCount;$("orcCertified").textContent=s.certifiedCount;$("orcTaskSuccess").textContent=`${s.taskQuality.successRate}%`;$("orcResolution").textContent=s.taskQuality.averageResolutionMinutes==null?"—":`${s.taskQuality.averageResolutionMinutes}m`;$("orcHeadline").textContent=`${s.candidateSet.eligibleCount} retirement candidate(s) currently satisfy preview evidence, owner sign-off, and workflow safety.`;$("orcList").innerHTML=s.candidateSet.items.map(x=>`<article><strong>${esc(x.surfaceId)} · ${esc(x.status)}</strong><span>${x.observedDays} observed day(s) · ${x.observations} observation(s) · owner ${x.ownerSignoff?esc(x.ownerSignoff.owner):"missing"}</span><div>${x.checks.map(c=>`<small>${c.pass?"PASS":"BLOCK"} ${esc(c.id)} — ${esc(c.detail)}</small>`).join("")}</div></article>`).join("")||"<p>No preview candidates in the manifest.</p>";}
+function load(){render(e.snapshot());}
+$("orcObserve")?.addEventListener("click",()=>{try{e.recordObservation(selected(),{note:$("orcNote").value||""});load();}catch(err){$("orcHeadline").textContent=err.message;}});$("orcSignoff")?.addEventListener("click",()=>{try{e.ownerSignoff(selected(),$("orcOwner").value||"Owner","approve",$("orcNote").value||"");load();}catch(err){$("orcHeadline").textContent=err.message;}});$("orcRevalidate")?.addEventListener("click",()=>{try{manifest.revalidate();load();}catch(err){$("orcHeadline").textContent=err.message;}});$("orcCertify")?.addEventListener("click",()=>{try{e.certify(selected(),$("orcOwner").value||"Certifier");load();}catch(err){$("orcHeadline").textContent=err.message;}});$("orcRefresh")?.addEventListener("click",load);load();return{engine:e,load};}
+window.createBlueCurrentRetirementCertificationCenterModule=createBlueCurrentRetirementCertificationCenterModule;})();--- a/scripts/maintenance/test-operator-retirement-certification.js
+++ b/scripts/maintenance/test-operator-retirement-certification.js
@@ -0,0 +1,28 @@
+"use strict";
+const assert=require("assert");
+global.structuredClone=global.structuredClone||((x)=>JSON.parse(JSON.stringify(x)));
+const Rational=require("../../client/js/modules/operatorSurfaceRationalizationEngine");
+const Consolidation=require("../../client/js/modules/operatorConsolidationEngine");
+const Scorecard=require("../../client/js/modules/operatorConsolidationScorecardEngine");
+const Manifest=require("../../client/js/modules/deprecationManifestEngine");
+const Retirement=require("../../client/js/modules/retirementCertificationEngine");
+const state={roleExperienceRole:"manager",operatorUseEvidence:[]};
+const appState={getState:()=>state,get:k=>state[k],update:o=>Object.assign(state,o)},eventBus={emit:()=>{},on:()=>()=>{}};
+const rational=new Rational({eventBus,appState}),consolidation=new Consolidation({eventBus,appState,rationalizationEngine:rational});
+const fakeFlow={snapshot:()=>({handoffs:[{status:"clear"},{status:"clear"},{status:"clear"},{status:"clear"},{status:"clear"}]})};
+const score=new Scorecard({eventBus,appState,consolidationEngine:consolidation,rationalizationEngine:rational,flowEngine:fakeFlow});
+const manifest=new Manifest({eventBus,appState,consolidationEngine:consolidation,rationalizationEngine:rational,scorecardEngine:score});
+const retirement=new Retirement({eventBus,appState,consolidationEngine:consolidation,manifestEngine:manifest,scorecardEngine:score});
+consolidation.recordTaskStart("reservationOperationsCenter","seat-1",{at:"2026-08-05T18:00:00Z"});
+consolidation.recordTaskCompletion("reservationOperationsCenter","seat-1","completed",{startedAt:new Date(Date.now()-60000).toISOString()});
+let m=manifest.createDraft(["innovationCenter"]);m=manifest.approve("GM");let rv=manifest.revalidate();assert.equal(rv.valid,true);m=manifest.setFeatureFlag(true);assert.equal(m.featureFlagEnabled,true);
+retirement.recordObservation("innovationCenter",{at:"2026-08-05T20:00:00Z",workflowCoverage:100});
+retirement.recordObservation("innovationCenter",{at:"2026-08-06T20:00:00Z",workflowCoverage:100});
+retirement.recordObservation("innovationCenter",{at:"2026-08-07T20:00:00Z",workflowCoverage:100});
+retirement.ownerSignoff("innovationCenter","Product Owner","approve","Preview stable");
+const cert=retirement.surfaceCertification("innovationCenter");assert.equal(cert.eligible,true);assert.equal(cert.observedDays,3);assert.equal(cert.observations,3);assert.equal(cert.ownerSignoff.decision,"approve");
+const rec=retirement.certify("innovationCenter","GM Tester");assert.equal(rec.status,"retirement-candidate-certified");assert.equal(rec.codeDeletionAllowed,false);
+const snap=retirement.snapshot();assert.equal(snap.certifiedCount,1);assert.equal(snap.automaticDeletion,false);assert.equal(snap.codeDeletionAllowed,false);
+retirement.recordObservation("innovationCenter",{at:"2026-08-08T20:00:00Z",workflowCoverage:100,regressions:1});
+const degraded=retirement.surfaceCertification("innovationCenter");assert.equal(degraded.eligible,false);assert.ok(degraded.checks.find(x=>x.id==="regressions"&&!x.pass));
+console.log(JSON.stringify({ok:true,version:snap.version,observedDays:cert.observedDays,previewObservations:cert.observations,taskSuccess:cert.taskQuality.successRate,averageResolutionMinutes:cert.taskQuality.averageResolutionMinutes,ownerSignoff:cert.ownerSignoff.owner,certified:rec.status,codeDeletionAllowed:rec.codeDeletionAllowed,regressionBlocksRetirement:!degraded.eligible,policy:snap.policy},null,2));
--- a/README-APPLY-V46.30.0.md
+++ b/README-APPLY-V46.30.0.md
@@ -0,0 +1,65 @@
+# APPLY BLUE CURRENT V46.30.0
+
+## Baseline
+Apply over the authoritative repository containing V46.25.0.
+
+## Important
+This patch does NOT contain:
+- `database/data/blue-current.json`
+- database `.tmp` files
+- database `.bak` files
+
+## Apply
+Extract `BLUE-CURRENT-V46.30.0-PATCH.zip` into the root of `blue-current-reservations`, preserving directories and replacing matching files.
+
+## Validate
+```powershell
+taskkill /F /IM node.exe
+npm run check
+node scripts/maintenance/test-operator-retirement-certification.js
+npm run start
+```
+
+In another PowerShell window:
+
+```powershell
+curl.exe -s http://localhost:8787/api/health
+```
+
+Expected:
+
+```json
+"version": "46.30.0"
+```
+
+## Recommended Git staging
+```powershell
+git add package.json
+git add server/server.js
+git add server/api/router.js
+git add client/index.html
+git add client/js/app-v15.1.3.js
+git add client/js/startup-loader.js
+git add client/js/modules/operatorConsolidationEngine.js
+git add client/js/modules/deprecationManifestEngine.js
+git add client/js/modules/retirementCertificationEngine.js
+git add client/js/modules/retirementCertificationCenter.js
+git add scripts/maintenance/test-operator-retirement-certification.js
+git add README-APPLY-V46.30.0.md
+git add V46.30.0-RELEASE.md
+git add V46.30.0-IMPLEMENTATION.patch
+```
+
+Inspect:
+
+```powershell
+git status
+git diff --cached
+```
+
+Commit example:
+
+```powershell
+git commit -m "V46.30.0 retirement certification"
+git push origin live-service-timeline
+```
--- a/V46.30.0-RELEASE.md
+++ b/V46.30.0-RELEASE.md
@@ -0,0 +1,129 @@
+# BLUE CURRENT V46.30.0 — RETIREMENT CERTIFICATION
+
+## Release scope
+
+V46.26–V46.30 strengthens the fine-comb process so a hidden-preview surface cannot become a retirement candidate without multi-day evidence, owner sign-off, fresh manifest validation, and clean workflow results.
+
+No code is permanently deleted.
+
+### V46.26 — Multi-Day Preview Evidence
+Adds persistent preview observations for surfaces hidden behind an approved deprecation feature flag.
+
+Each observation records:
+- surface ID
+- timestamp
+- manager workflow coverage
+- task failures
+- regressions
+- operator note
+- preview-hidden state
+
+Retirement certification requires observations across at least 3 distinct days and at least 3 preview snapshots.
+
+### V46.27 — Task Outcome + Time-to-Resolution Metrics
+Operator evidence now supports:
+- task-started events
+- task-completed events
+- success/failure outcomes
+- duration measurement
+- per-role completion metrics
+- success rate
+- average resolution time
+
+Current retirement thresholds require:
+- at least 95% task success
+- no more than 15 minutes average resolution where duration evidence exists
+
+### V46.28 — Manifest Expiry + Revalidation
+Deprecation manifests now:
+- expire after 72 hours
+- require revalidation before hide preview can be enabled
+- recheck manager workflow coverage
+- recheck candidate eligibility
+- record last revalidation time/status
+
+An expired or invalid manifest cannot enable preview hiding.
+
+### V46.29 — Owner Sign-Off
+Each retirement candidate requires explicit owner sign-off.
+
+The sign-off records:
+- surface
+- owner
+- decision
+- note
+- timestamp
+
+Missing or non-approved owner sign-off blocks retirement eligibility.
+
+### V46.30 — Retirement Candidate Certification
+Adds a separate retirement-certification gate.
+
+A surface must pass:
+- approved manifest
+- valid/revalidated manifest
+- active reversible hide preview
+- 3 observed days
+- 3 preview observations
+- 100% workflow coverage throughout preview
+- zero preview regressions/failures
+- task-success threshold
+- time-to-resolution threshold
+- owner approval
+
+Passing surfaces can become:
+
+`retirement-candidate-certified`
+
+This status still explicitly sets:
+- `automaticDeletion: false`
+- `codeDeletionAllowed: false`
+
+A later regression causes the surface to fail retirement eligibility again.
+
+## UI
+Adds the Retirement Certification center with:
+- candidate selector
+- preview observation capture
+- owner sign-off
+- manifest revalidation
+- candidate certification
+- eligible/certified counts
+- task success rate
+- average resolution time
+- per-candidate certification checks
+
+## Validation
+- `node scripts/maintenance/test-operator-retirement-certification.js` passed.
+- 3 distinct preview days validated.
+- 3 preview observations validated.
+- Task success measured at 100% in focused harness.
+- Average resolution measured at 1 minute.
+- Owner sign-off validated.
+- Retirement candidate certification succeeded.
+- `codeDeletionAllowed` remained false.
+- Injected regression immediately blocked retirement eligibility.
+- `npm run check` passed:
+  - 828 JavaScript files
+  - 710 client scripts
+  - 5,294 unique HTML IDs
+- Server startup passed as V46.30.0.
+- `/api/health` returned HTTP 200 with database connected and authentication enabled.
+- Authoritative runtime database SHA-256 remained unchanged.
+
+## Fine-comb policy
+`certify-retirement-never-auto-delete`
+
+This release creates evidence-backed retirement candidates only. Permanent deletion remains a later explicit decision after human review and repository-impact analysis.
+
+## Next milestone
+The next fine-comb wave should begin repository-impact analysis for certified candidates:
+- dependency/reference graph
+- API/client usage checks
+- startup-registry dependency checks
+- HTML/script ownership
+- test coverage impact
+- rollback package generation
+- human deletion authorization
+
+The result should be a deletion plan, not deletion itself.
