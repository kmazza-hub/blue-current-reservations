# Apply V100.3.7

Copy this overlay onto the current V100.3.6 repository, preserving paths and allowing replacement of existing files.

Then restart Blue Current and hard-refresh the iPad/browser.

Primary acceptance sequence: Find guest → Service → Kitchen → Staff → Back/Exit → Floor → Full screen floor → Seat.
