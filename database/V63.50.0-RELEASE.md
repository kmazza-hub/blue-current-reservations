# BLUE CURRENT V63.50.0 — FULL-SYSTEM USABILITY VERIFICATION & RUNTIME GUARDRAILS

Product-quality wave, not a feature expansion.

The static audit confirms zero duplicate HTML IDs and zero broken hash-navigation targets.

Adds:
- resilient direct links into hidden Tools, Advanced, AI Advanced, and Supporting Insight surfaces
- automatic hidden-parent reveal before navigation
- fallback accessible names for controls missing a usable label
- meaningful semantics/tooltips for unavailable primary-workspace values
- focus return when dialogs close
- active top-navigation state that follows the visible workspace
- 44px minimum primary touch targets and readable primary form controls
- high-contrast browser preference support
- production-facing cleanup of engineering-version prefixes in primary eyebrow labels
- a nonvisual BlueCurrentUsability runtime QA interface

No API, workflow, business rule, or operator capability is removed.
