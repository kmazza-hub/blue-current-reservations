(function(){"use strict";function createBlueCurrentBackgroundActivityGovernorCenterModule(eventBus,appState){const root=document.getElementById("backgroundActivityGovernorCenter");if(!root||!window.BlueCurrentBackgroundActivityGovernorEngine)return null;const engine=new window.BlueCurrentBackgroundActivityGovernorEngine({eventBus,appState}),$=id=>document.getElementById(id);function render(v=engine.snapshot()){$("bagState").textContent=v.status;$("bagIntervals").textContent=v.managedIntervals;$("bagSkipped").textContent=v.skippedCallbacks.toLocaleString();$("bagRate").textContent=`${Math.round(v.estimatedCallbacksPerMinute)}/min`;$("bagHeadline").textContent=v.hidden?"Background activity is paused while this tab is hidden.":"Foreground activity is running under one managed governor.";$("bagChecks").innerHTML=[{label:"Hidden-tab protection",pass:true,detail:"Recurring callbacks do not execute while the tab is hidden."},{label:"Managed interval inventory",pass:v.managedIntervals<80,detail:`${v.managedIntervals} recurring intervals are currently registered.`},{label:"Estimated activity budget",pass:v.estimatedCallbacksPerMinute<900,detail:`Approximately ${Math.round(v.estimatedCallbacksPerMinute)} callbacks per minute while visible.`}].map(x=>`<article data-pass="${x.pass}"><div><strong>${x.label}</strong><span>${x.detail}</span></div><b>${x.pass?"PASS":"WATCH"}</b></article>`).join("");}document.getElementById("bagRefresh").onclick=()=>render(engine.refresh("manual"));eventBus.on("background-activity-governor:updated",render);render();return{engine,refresh:render,destroy:()=>engine.destroy()};}window.createBlueCurrentBackgroundActivityGovernorCenterModule=createBlueCurrentBackgroundActivityGovernorCenterModule;})();