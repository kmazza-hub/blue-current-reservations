(function(){"use strict";
function createBlueCurrentDecisionHorizonForecastCenterModule(eventBus){const root=document.getElementById("decisionHorizonForecastCenter");if(!root||!window.BlueCurrentDecisionHorizonForecastEngine)return null;const engine=new window.BlueCurrentDecisionHorizonForecastEngine(eventBus),$=id=>document.getElementById(id),esc=v=>String(v??"").replace(/[&<>\"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"}[c]));
 function render(r){if(!r)return;$("horizonConfidence").textContent=`${r.confidence}%`;$("horizonStatus").textContent=r.status;$("horizonRisks").textContent=r.risks.length;$("horizonWindow").textContent=`${r.minutes} min`;$("horizonMessage").textContent=r.risks[0]||"No material operating risk is projected in the selected horizon.";$("horizonList").innerHTML=Object.entries(r.projected).map(([k,v])=>`<article><strong>${esc(k.replace(/([A-Z])/g," $1"))}</strong><span>${esc(v)}</span></article>`).join("")+r.risks.map(x=>`<article class="risk"><strong>Risk</strong><span>${esc(x)}</span></article>`).join("");}
 $("horizonRun")?.addEventListener("click",()=>render(engine.forecast(Number($("horizonMinutes")?.value)||60)));const last=engine.history()[0];if(last)render(last);return{engine,render};}
window.createBlueCurrentDecisionHorizonForecastCenterModule=createBlueCurrentDecisionHorizonForecastCenterModule;})();