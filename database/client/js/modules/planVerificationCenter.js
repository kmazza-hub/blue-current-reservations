(function(){"use strict";
function createBlueCurrentPlanVerificationCenterModule(eventBus){const root=document.getElementById("planVerificationCenter");if(!root||!window.BlueCurrentPlanVerificationEngine)return null;const engine=new window.BlueCurrentPlanVerificationEngine(eventBus),$=id=>document.getElementById(id),esc=v=>String(v??"").replace(/[&<>\"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"}[c]));
 function render(r){if(!r)return;$("verifyScore").textContent=`${r.score}%`;$("verifyStatus").textContent=r.status;$("verifyBlockers").textContent=r.blockers;$("verifyId").textContent=r.id;$("verifyMessage").textContent=r.blockers?`${r.blockers} blocker(s) remain before governed execution.`:"Plan is verified against the current twin and constraints.";$("verifyChecks").innerHTML=r.checks.map(x=>`<article class="${x.pass?"pass":"fail"}"><strong>${x.pass?"PASS":"BLOCK"}</strong><div><b>${esc(x.name)}</b><span>${esc(x.detail)}</span></div></article>`).join("");}
 $("verifyRun")?.addEventListener("click",()=>render(engine.verify($("verifyOwner")?.value||"Manager")));return{engine,render};}
window.createBlueCurrentPlanVerificationCenterModule=createBlueCurrentPlanVerificationCenterModule;})();