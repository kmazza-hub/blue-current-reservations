(function(){"use strict";
function createBlueCurrentAgentNegotiationCenterModule(eventBus){const root=document.getElementById("agentNegotiationCenter");if(!root||!window.BlueCurrentAgentNegotiationEngine)return null;const e=new window.BlueCurrentAgentNegotiationEngine(eventBus),$=id=>document.getElementById(id);
function render(r){if(!r)return;$("agentNegotiationStatus").textContent=r.status;$("agentNegotiationConsensus").textContent=`${r.consensus}%`;$("agentNegotiationSpread").textContent=`${r.spread} pts`;$("agentNegotiationId").textContent=r.id;$("agentNegotiationList").innerHTML=(r.positions||[]).map(x=>`<article class="${x.support>=80?'preferred':x.support>=65?'medium':'high'}"><div><strong>${x.agent}</strong><span>${x.position}</span><small>Priority: ${x.priority} · Support ${x.support}%</small></div><b>${x.support}%</b></article>`).join("");}
$("agentNegotiationRun")?.addEventListener("click",()=>render(e.negotiate($("agentNegotiationOwner")?.value)));$("agentNegotiationExport")?.addEventListener("click",()=>{const r=e.history()[0];if(!r)return;const a=document.createElement("a");a.href=URL.createObjectURL(new Blob([JSON.stringify(r,null,2)],{type:"application/json"}));a.download=`${r.id}.json`;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000);});const last=e.history()[0];if(last)render(last);return{engine:e,render};}
window.createBlueCurrentAgentNegotiationCenterModule=createBlueCurrentAgentNegotiationCenterModule;})();