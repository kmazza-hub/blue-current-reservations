(function(){"use strict";
function createBlueCurrentConstraintPropagationCenterModule(eventBus){const root=document.getElementById("constraintPropagationCenter");if(!root||!window.BlueCurrentConstraintPropagationEngine)return null;const engine=new window.BlueCurrentConstraintPropagationEngine(eventBus),$=id=>document.getElementById(id),esc=v=>String(v??"").replace(/[&<>\"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"}[c]));
 function render(r){if(!r)return;$("propConfidence").textContent=`${r.confidence}%`;$("propStatus").textContent=r.status;$("propEffects").textContent=r.effects.length;$("propRisk").textContent=r.highRisk;$("propMessage").textContent=`${r.change} propagates across ${r.effects.length} operating effects.`;$("propList").innerHTML=r.effects.map(x=>`<article data-risk="${esc(x.risk)}"><div><strong>${esc(x.target)}</strong><small>${esc(x.direction)} · ${esc(x.amount)}${x.dependency?" strength":""}</small></div><span>${esc(x.risk)}</span></article>`).join("");}
 $("propRun")?.addEventListener("click",()=>render(engine.propagate($("propChange")?.value,$("propMagnitude")?.value)));eventBus?.on?.("aip:counterfactual-complete",e=>render(engine.propagate(e?.scenario||"Scenario",e?.magnitude||10)));return{engine,render};}
window.createBlueCurrentConstraintPropagationCenterModule=createBlueCurrentConstraintPropagationCenterModule;})();