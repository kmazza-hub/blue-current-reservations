(function(){"use strict";function createBlueCurrentAssetHealthCenterModule(eventBus,appState){const root=document.getElementById("assetHealthCenter");if(!root||!window.BlueCurrentAssetHealthEngine)return null;const engine=new window.BlueCurrentAssetHealthEngine({eventBus,appState}),$=id=>document.getElementById(id);function render(v=engine.snapshot()){$("ahScore").textContent=`${v.score}%`;$('ahStatus').textContent=v.status;$('ahLoaded').textContent=v.loadedScripts;$('ahDeferred').textContent=v.deferredScripts;$('ahHeadline').textContent=v.status==="healthy"?"The current startup profile is inside the asset-health budget.":"Review duplicate or slow assets before expanding this startup profile.";$('ahChecks').innerHTML=[{label:"Duplicate JavaScript",pass:!v.duplicateScripts.length,detail:v.duplicateScripts.length?`${v.duplicateScripts.length} duplicate asset paths detected.`:"No duplicate loaded asset paths detected."},{label:"Slow JavaScript",pass:!v.slowScripts.length,detail:v.slowScripts.length?`${v.slowScripts.length} scripts exceeded 1.2 seconds.`:"No JavaScript asset exceeded the slow-load threshold."},{label:"Startup guard",pass:v.bootStatus==="complete",detail:`Boot guard status: ${v.bootStatus}.`},{label:"Script budget",pass:v.loadedScripts<=70,detail:`${v.loadedScripts} JavaScript resources loaded in ${v.mode} mode.`}].map(x=>`<article data-pass="${x.pass}"><div><strong>${x.label}</strong><span>${x.detail}</span></div><b>${x.pass?"PASS":"WATCH"}</b></article>`).join("");}$("ahRefresh").onclick=()=>render(engine.refresh("manual"));$("ahExport").onclick=()=>engine.export();eventBus.on("asset-health:updated",render);window.addEventListener("bluecurrent:boot-complete",()=>setTimeout(()=>render(engine.refresh("boot-complete")),0),{once:true});render();return{engine,refresh:render};}window.createBlueCurrentAssetHealthCenterModule=createBlueCurrentAssetHealthCenterModule;})();