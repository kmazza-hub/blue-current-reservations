# Apply V100.3.47

Overlay this package, run `npm install`, `npm run check`, the V100.3.47 test, and `npm run certify:pilot`. Keep live database files unstaged.

Start Blue Current, sign in as an administrator, and append `?pilotSetup=1` to the application URL.
