# Apply V36.20.0

Replace the four existing client files and add the six modules included in this patch.

Then run:

```powershell
git add .
git commit -m "V36.20 accelerated wave"
git pull --rebase origin live-service-timeline
git push origin live-service-timeline
```
