# Apply Blue Current V36.14.0

Replace the four existing client files and add the six new modules and two documentation files using the same relative paths.

Then run:

```powershell
git add .
git commit -m "V36.14 accelerated command wave"
git pull --rebase origin live-service-timeline
git push origin live-service-timeline
```
