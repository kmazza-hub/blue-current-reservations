# Apply V100.3.6

Copy this patch over the current V100.3.5 repository, preserving paths and allowing overwrite of `client/index.html` and `client/styles.css`.

Then restart Blue Current. For LAN iPad testing, keep the same allowed origin used during physical-device acceptance.

V100.3.6 replaces scroll-target Quick Jobs with fixed operator workspaces for Find Guest, Service, Kitchen, and Staff. Full-screen Host Floor behavior is retained.
