# APPLY WEB-030

Copy this ZIP into the repository root and replace the included files.

```powershell
node scripts/maintenance/test-web-030-hospitality-os-story.js
npm run check
npm run start
```

Review the public homepage before committing.

```powershell
git add -A
git commit -m "WEB-030 expand hospitality OS story"
git push origin live-service-timeline
```
