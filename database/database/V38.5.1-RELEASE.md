# Blue Current V38.5.1 — Startup Hotfix

- Operations pack loads only explicitly assigned V38.5 modules.
- Legacy operations modules remain available in full mode instead of starting eagerly.
- Non-active centers are hidden and inert before application initialization.
- Script loads have hard timeouts and yield between modules.
- Safe startup initializes only the essential operator shell.
- Adds `window.BlueCurrentBootReport` for diagnostics.
