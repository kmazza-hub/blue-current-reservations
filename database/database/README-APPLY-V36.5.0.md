# Apply Blue Current V36.5.0

Replace the four existing client files and add the six modules plus release note.

```powershell
git add .
git commit -m "V36.5 accelerated operator experience"
git pull --rebase origin live-service-timeline
git push origin live-service-timeline
```
