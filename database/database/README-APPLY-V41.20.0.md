# Apply V41.20.0

Replace the four existing client files and add the six new modules included in this patch.

Restart with:

```powershell
taskkill /F /IM node.exe
npm run start
```

Open `http://localhost:8787/?pack=aip`.
