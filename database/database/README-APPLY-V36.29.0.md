# Apply V36.29.0

Replace the four existing client files and add the six new modules plus this release documentation. Preserve the directory structure exactly.

```powershell
git add .
git commit -m "V36.29 supplier prep and weekly profit review"
git pull --rebase origin live-service-timeline
git push origin live-service-timeline
```
