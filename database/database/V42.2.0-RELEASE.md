# Blue Current V42.2.0 — Live Operations Runtime

## V42.0 Live Connector Runtime
Adds authenticated server-side connector metadata, health testing, and realtime connector events. No credentials are stored in the browser.

## V42.1 Canonical Event Gateway
Adds a durable authenticated ingestion endpoint for normalized restaurant events and publishes accepted events through the existing realtime hub.

## V42.2 Live Source Health
Reconciles connector health, event freshness, and recent event throughput so reasoning can distinguish current data from stale or missing sources.
