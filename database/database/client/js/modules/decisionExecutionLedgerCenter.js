(function(){"use strict";
function createBlueCurrentDecisionExecutionLedgerCenterModule(eventBus){const root=document.getElementById("decisionExecutionLedgerCenter");if(!root||!window.BlueCurrentDecisionExecutionLedgerEngine)return null;const engine=new window.BlueCurrentDecisionExecutionLedgerEngine({eventBus}),$=id=>document.getElementById(id),esc=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));function render(v=engine.snapshot()){if($("delTotal"))$("delTotal").textContent=v.total;if($("delAssigned"))$("delAssigned").textContent=v.assigned;if($("delStarted"))$("delStarted").textContent=v.started;if($("delCompleted"))$("delCompleted").textContent=v.completed;if($("delHeadline"))$("delHeadline").textContent=v.last?`Latest event: ${v.last.event} · ${new Date(v.last.occurredAt).toLocaleTimeString([], {hour:"numeric",minute:"2-digit"})}`:"No execution events recorded yet.";if($("delRows"))$("delRows").innerHTML=v.rows.slice(0,20).map(x=>`<article><div><small>${esc(x.event)}</small><strong>${esc(x.priorityId)}</strong><span>${esc(x.owner||"Manager")} · ${esc(new Date(x.occurredAt).toLocaleString())}</span></div></article>`).join("");}$("delRefresh")?.addEventListener("click",()=>render());$("delClear")?.addEventListener("click",()=>engine.clear());eventBus.on("decision-execution-ledger:updated",render);render();return{engine,refresh:render};}
window.createBlueCurrentDecisionExecutionLedgerCenterModule=createBlueCurrentDecisionExecutionLedgerCenterModule;})();
