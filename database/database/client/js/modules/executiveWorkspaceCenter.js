(function(){"use strict";
function createBlueCurrentExecutiveWorkspaceCenterModule(eventBus){const root=document.getElementById("executiveWorkspaceCenter");if(!root||!window.BlueCurrentExecutiveWorkspaceEngine)return null;const e=new window.BlueCurrentExecutiveWorkspaceEngine(eventBus),$=id=>document.getElementById(id);
function render(r){if(!r)return;$("executiveWorkspaceScore").textContent=`${r.score}%`;$("executiveWorkspaceStatus").textContent=r.status;$("executiveWorkspaceStrategy").textContent=r.recommendedStrategy;$("executiveWorkspaceBlockers").textContent=r.blockers;$("executiveWorkspaceId").textContent=r.id;$("executiveWorkspaceChecks").innerHTML=r.checks.map(x=>`<article class="${x.pass?'pass':'block'}"><span>${x.name}</span><b>${x.pass?'Pass':'Block'}</b></article>`).join("");}
$("executiveWorkspaceRun")?.addEventListener("click",()=>render(e.build($("executiveWorkspaceOwner").value)));$("executiveWorkspaceExport")?.addEventListener("click",()=>{const r=e.history()[0];if(!r)return;const a=document.createElement("a");a.href=URL.createObjectURL(new Blob([JSON.stringify(r,null,2)],{type:"application/json"}));a.download=`${r.id}.json`;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000);});const last=e.history()[0];if(last)render(last);return{engine:e,render};}
window.createBlueCurrentExecutiveWorkspaceCenterModule=createBlueCurrentExecutiveWorkspaceCenterModule;})();