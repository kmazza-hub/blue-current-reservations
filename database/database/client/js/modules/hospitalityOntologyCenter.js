(function(){"use strict";
function createBlueCurrentHospitalityOntologyCenterModule(eventBus){const root=document.getElementById("hospitalityOntologyCenter");if(!root||!window.BlueCurrentHospitalityOntologyEngine)return null;const engine=new window.BlueCurrentHospitalityOntologyEngine(eventBus),$=id=>document.getElementById(id),esc=v=>String(v??"").replace(/[&<>\"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"}[c]));
 function render(){const s=engine.summary();$("ontTotal").textContent=s.total;$("ontActive").textContent=s.active;$("ontDraft").textContent=s.draft;$("ontTypes").textContent=s.types;$("ontList").innerHTML=engine.entities.map(e=>`<article data-status="${e.status}"><div><strong>${esc(e.name)}</strong><span>${esc(e.type)} · ${esc(e.owner)}</span><small>${esc(e.description||"No description")}</small></div><button type="button" data-id="${e.id}" data-next="${e.status==="active"?"retired":"active"}">${e.status==="active"?"Retire":"Activate"}</button></article>`).join("");}
 $("ontForm")?.addEventListener("submit",e=>{e.preventDefault();try{engine.add({name:$("ontName")?.value,type:$("ontType")?.value,description:$("ontDescription")?.value,owner:$("ontOwner")?.value});e.target.reset();$("ontMessage").textContent="Ontology entity saved as draft.";render();}catch(err){$("ontMessage").textContent=err.message;}});
 $("ontList")?.addEventListener("click",e=>{const b=e.target.closest("button[data-id]");if(!b)return;engine.setStatus(b.dataset.id,b.dataset.next);render();});render();return{engine,render};}
window.createBlueCurrentHospitalityOntologyCenterModule=createBlueCurrentHospitalityOntologyCenterModule;})();