# V33.0.1 Migration

No external database is required. The JSON database gains four collections: `workforceRoles`, `employeeAvailability`, `ptoRequests`, and `shiftTemplates`. Existing staff records remain compatible. Run `node database/migrations/011_workforce_foundation.js` only when your deployment pipeline explicitly invokes migrations; the packaged database is already upgraded.
