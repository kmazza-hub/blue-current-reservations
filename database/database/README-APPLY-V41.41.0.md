# Apply V41.41.0

Replace the four active files and add the six new modules included in this patch.

Restart:

```powershell
taskkill /F /IM node.exe
npm run start
```

Open `http://localhost:8787/?pack=aip`. Run Enterprise Decision Console, then Live Evidence Reconciliation, then V41 Closure Certification.
