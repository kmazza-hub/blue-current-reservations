# Apply V36.17.0

Replace `client/index.html`, `client/styles.css`, `client/js/appState.js`, and `client/js/app-v15.1.3.js`. Add the six new modules under `client/js/modules/`. Then run `git add .`, commit, pull with rebase, and push.
