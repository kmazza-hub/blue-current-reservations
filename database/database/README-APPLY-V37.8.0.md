# Apply V37.8.0

Replace the five existing files and add the six new modules plus release documentation. Restart Node after copying. Start with `http://localhost:8787/`; use saved startup profiles only after focused mode is confirmed healthy.
