# Apply V37.23.0

Replace the included client files and add the six new runtime modules. Restart Node, then test `http://localhost:8787/` in focused mode. Capture one baseline, make a second capture after normal use, run regression control, then run the production runtime gate.
