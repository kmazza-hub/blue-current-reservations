--- a/package.json
+++ b/package.json
@@ -1,8 +1,8 @@
 {
   "name": "blue-current-cloud",
-  "version": "45.30.0",
+  "version": "46.5.0",
   "private": true,
-  "description": "Blue Current Hospitality OS \u2014 V45 failure recovery certification and architecture closure",
+  "description": "Blue Current Hospitality OS \u2014 operator fine-comb service flow and surface simplification",
   "scripts": {
     "start": "node server/server.js",
     "check": "node scripts/validate.js"
--- a/server/server.js
+++ b/server/server.js
@@ -152,7 +152,7 @@
 });
 
 authService.initializePasswords().then(() => server.listen(PORT, () => {
-  console.log(`Blue Current Cloud V45.30.0 running at http://localhost:${PORT}`);
+  console.log(`Blue Current Cloud V46.5.0 running at http://localhost:${PORT}`);
   console.log(`Database: ${DB_PATH}`);
 })).catch(error => {
   console.error(error);
--- a/server/api/router.js
+++ b/server/api/router.js
@@ -74,7 +74,7 @@
     if (url.pathname === "/api/health" && request.method === "GET") {
       return sendJson(response, 200, {
         ok: true,
-        version: "45.30.0",
+        version: "46.5.0",
         database: "connected",
         auth: "enabled",
         realtimeClients: realtimeHub.count(),
--- a/client/index.html
+++ b/client/index.html
@@ -1,7 +1,7 @@
 <!doctype html>
 <html lang="en">
 <head>
-  <meta name="blue-current-build" content="45.30.0">
+  <meta name="blue-current-build" content="46.5.0">
   <meta charset="utf-8">
   <meta name="viewport" content="width=device-width, initial-scale=1">
   <title>Blue Current | Hospitality That Answers</title>
@@ -4162,6 +4162,7 @@
 <section class="v3730-center v3730-cert" id="v37CertificationCenter" aria-labelledby="v37cTitle"><header><div><small>V37.30 · Final V37 Certification</small><strong id="v37cTitle">Close V37 with one production-readiness certificate.</strong><span id="v37cHeadline">Waiting for final certification…</span></div><div class="v3730-actions"><button type="button" id="v37cRun">Run certification</button><button type="button" id="v37cExport">Export certificate</button></div></header><div class="v3730-kpis"><article><small>Certification score</small><strong id="v37cScore">0%</strong></article><article><small>Status</small><strong id="v37cStatus">—</strong></article><article><small>Checks passed</small><strong id="v37cPassed">0/0</strong></article><article><small>Certificate</small><strong id="v37cId">Pending</strong></article></div><div id="v37cChecks" class="v3730-list"></div></section>
 
 
+<section class="v393-center" id="operatorServiceFlowCenter" aria-labelledby="osfTitle"><header><div><small>V46.1–V46.5 · Operator Fine-Comb I</small><strong id="osfTitle">Run the shift through one service flow instead of hunting across the platform.</strong><span id="osfHeadline">Consolidating reservation, floor, kitchen, staffing, guest recovery, and profit signals.</span></div><div class="v393-actions"><button id="osfRefresh" type="button">Refresh flow</button><button id="osfOperatorMode" type="button">Operator mode</button><button id="osfFullMode" type="button">Full platform</button></div></header><div class="v393-kpis"><article><small>Flow score</small><strong id="osfScore">0%</strong></article><article><small>Live stages</small><strong id="osfLive">0/6</strong></article><article><small>Needs attention</small><strong id="osfAttention">0</strong></article><article><small>Total surfaces</small><strong id="osfSurfaces">0</strong></article><article><small>Technical surfaces</small><strong id="osfTechnical">0</strong></article><article><small>View</small><strong id="osfMode">FULL</strong></article></div><div id="osfStages" class="v393-grid"></div><div id="osfAudit" class="v393-list"></div></section>
 <section class="guided-shift-center" id="guidedShiftCenter" aria-labelledby="guidedShiftTitle">
             <header class="guided-shift-header">
               <div><small>V36.1 · Guided Operator Flow</small><strong id="guidedShiftTitle">Stay ahead of the shift without hunting through dashboards.</strong><span id="guidedShiftUpdated">Building operator flow…</span></div>
@@ -8617,6 +8618,8 @@
 <script type="text/bluecurrent-deferred" data-pack="aip" data-pack-explicit="true" data-center="executionCertificationCenter" data-src="js/modules/executionCertificationCenter.js?v=45.25.0"></script>
 <script type="text/bluecurrent-deferred" data-pack="aip" data-pack-explicit="true" data-center="v45ClosureCenter" data-src="js/modules/v45ClosureEngine.js?v=45.30.0"></script>
 <script type="text/bluecurrent-deferred" data-pack="aip" data-pack-explicit="true" data-center="v45ClosureCenter" data-src="js/modules/v45ClosureCenter.js?v=45.30.0"></script>
-<script src="js/startup-loader.js?v=45.30.0"></script>
+<script src="js/modules/operatorServiceFlowEngine.js?v=46.5.0"></script>
+<script src="js/modules/operatorServiceFlowCenter.js?v=46.5.0"></script>
+<script src="js/startup-loader.js?v=46.5.0"></script>
 </body>
 </html>
--- a/client/js/app-v15.1.3.js
+++ b/client/js/app-v15.1.3.js
@@ -1209,6 +1209,8 @@
   ["eventBus", "appState", "unifiedCommandCenter"]
 );
 
+const operatorServiceFlowCenterModule = startupRegistry.register("operatorServiceFlowCenter", shouldInitializeCenter("operatorServiceFlowCenter") ? window.createBlueCurrentOperatorServiceFlowCenterModule?.(eventBus, appState) : null, ["eventBus", "appState", "guidedShiftCenter"]);
+
 const operatorCopilotCenterModule = startupRegistry.register(
   "operatorCopilotCenter",
   shouldInitializeCenter("operatorCopilotCenter")
@@ -2734,6 +2736,7 @@
     autonomousOperations: operationsDirectorModule,
     unifiedCommand: unifiedCommandCenterModule,
     guidedShift: guidedShiftCenterModule,
+    operatorServiceFlow: operatorServiceFlowCenterModule,
     operatorCopilot: operatorCopilotCenterModule,
     roleExperience: roleExperienceCenterModule,
     commandActionInbox: commandActionInboxCenterModule,
--- a/client/js/startup-loader.js
+++ b/client/js/startup-loader.js
@@ -118,7 +118,7 @@
     document.querySelectorAll("[id$='Center'], [id$='center']").forEach((center) => {
       if (!center.id) return;
       const isEssential = [
-        "unifiedCommandCenter", "guidedShiftCenter", "operatorCopilotCenter",
+        "unifiedCommandCenter", "guidedShiftCenter", "operatorServiceFlowCenter", "operatorCopilotCenter",
         "roleExperienceCenter", "commandActionInboxCenter", "shiftProfitPulseCenter",
         "featurePackLoaderCenter", "bootRecoveryCenter"
       ].includes(center.id);
@@ -209,7 +209,7 @@
       }));
       const summary = document.getElementById("startupDiagnosticsSummary");
       const dot = document.getElementById("startupDiagnosticsDot");
-      if (summary) summary.textContent = `V45.30.0 ready · ${duration}ms`;
+      if (summary) summary.textContent = `V46.5.0 ready · ${duration}ms`;
       if (dot) dot.className = "ok";
       // Local development can be held open by optional third-party assets. Once the
       // application is ready, stop those nonessential pending resource loads.
--- a/client/js/modules/operatorServiceFlowEngine.js
+++ b/client/js/modules/operatorServiceFlowEngine.js
@@ -0,0 +1,30 @@
+(function(global){"use strict";
+class BlueCurrentOperatorServiceFlowEngine{
+  constructor({eventBus,appState}){this.eventBus=eventBus;this.appState=appState;this.primaryCenters=new Set(["unifiedCommandCenter","guidedShiftCenter","operatorCopilotCenter","commandActionInboxCenter","shiftProfitPulseCenter","operatorServiceFlowCenter","operationsWorkspaceCenter","reservationOperationsCenter","liveFloorOperationsCenter","kitchenThroughputCenter","laborDeploymentCenter","guestRecoveryCenter"]);this.previousVisibility=new Map();this.off=[];["state:updated","guided-shift:updated","kitchen-throughput:updated","guest-recovery:updated","labor-deployment:updated","reservation-yield:updated","shift-profit-pulse:updated"].forEach(n=>this.off.push(eventBus?.on?.(n,()=>this.refresh(n))));}
+  stages(){return [
+    {id:"reservations",label:"Reservations",center:"reservationOperationsCenter",stateKeys:["reservationOperations","reservationYield","reservations"],question:"Are arrivals paced to capacity?"},
+    {id:"floor",label:"Floor",center:"liveFloorOperationsCenter",stateKeys:["liveFloorOperations","operationalDigitalTwin","occupancyPercent"],question:"Where is seating pressure building?"},
+    {id:"kitchen",label:"Kitchen",center:"kitchenThroughputCenter",stateKeys:["kitchenThroughput","kitchenOperations"],question:"What is constraining ticket flow?"},
+    {id:"staffing",label:"Staffing",center:"laborDeploymentCenter",stateKeys:["laborDeployment","staffOperations","activeStaff"],question:"Is labor aligned to demand?"},
+    {id:"guest",label:"Guest Recovery",center:"guestRecoveryCenter",stateKeys:["guestRecovery","guestRecoveryCases"],question:"Which guest moments need ownership?"},
+    {id:"profit",label:"Profit",center:"shiftProfitPulseCenter",stateKeys:["shiftProfitPulse","costVariance","marginIntelligence"],question:"Is the shift converting demand into profit?"}
+  ];}
+  state(){try{return this.appState?.getState?.()||{};}catch{return {};}}
+  signal(state,keys){for(const key of keys){const value=state[key];if(value!==undefined&&value!==null&&(!(Array.isArray(value))||value.length))return {key,value};}return null;}
+  stageSnapshot(stage,state){const signal=this.signal(state,stage.stateKeys),center=typeof document!=="undefined"?document.getElementById(stage.center):null;let status=signal?"live":center?"ready":"unavailable",summary="Awaiting operating signal.";
+    if(stage.id==="reservations"){const x=state.reservationYield||state.reservationOperations||{};summary=x.headline||`${Number(x.waitlist||x.waitlistCount||0)} waiting · ${Number(x.covers||x.bookedCovers||0)} covers`;}
+    if(stage.id==="floor"){const x=state.operationalDigitalTwin||state.liveFloorOperations||{};summary=`${Number(state.occupancyPercent??x.occupancy??0)}% occupied · ${Number(x.guestWait||0)} min guest wait`;}
+    if(stage.id==="kitchen"){const x=state.kitchenThroughput||{};summary=x.headline||`${Number(x.throughput||0)} throughput score · ${Number(x.wait||0)} min wait`; if(Number(x.throughput||100)<55)status="attention";}
+    if(stage.id==="staffing"){const x=state.laborDeployment||{};summary=x.headline||`${Number(state.activeStaff??x.activeStaff??0)} active staff · ${Number(x.laborPercent||0)}% labor`;}
+    if(stage.id==="guest"){const x=state.guestRecovery||{};summary=x.headline||`${Number(x.open||0)} open recovery moments`; if(Number(x.open||0)>0)status="attention";}
+    if(stage.id==="profit"){const x=state.shiftProfitPulse||state.costVariance||{};summary=x.headline||`${Number(x.margin||x.marginPercent||0)}% margin · $${Math.round(Number(x.profitAtRisk||0))} at risk`; if(Number(x.profitAtRisk||0)>0)status="attention";}
+    return {...stage,status,signalKey:signal?.key||null,centerPresent:Boolean(center),summary};}
+  surfaceAudit(){if(typeof document==="undefined")return {total:0,primary:0,supporting:0,hidden:0,items:[]};const centers=[...document.querySelectorAll('section[id$="Center"],section[id$="center"]')].filter(x=>x.id);const items=centers.map(el=>({id:el.id,primary:this.primaryCenters.has(el.id),hidden:Boolean(el.hidden),technical:/readiness|certification|registry|reconciliation|telemetry|governance|sandbox|runtime|observability|integration|contract|lineage|replay|audit|benchmark/i.test(el.id)}));return {total:items.length,primary:items.filter(x=>x.primary).length,supporting:items.filter(x=>!x.primary).length,hidden:items.filter(x=>x.hidden).length,technical:items.filter(x=>x.technical).length,items};}
+  snapshot(){const state=this.state(),stages=this.stages().map(x=>this.stageSnapshot(x,state)),live=stages.filter(x=>x.status==="live"||x.status==="attention").length,attention=stages.filter(x=>x.status==="attention").length,audit=this.surfaceAudit();const guided=state.guidedShift||{},inbox=state.commandActionInbox||{},profit=state.shiftProfitPulse||{};return {capturedAt:new Date().toISOString(),stages,liveStages:live,attentionStages:attention,flowScore:Math.round(stages.reduce((s,x)=>s+(x.status==="live"?100:x.status==="attention"?75:x.status==="ready"?50:0),0)/stages.length),surfaceAudit:audit,operator:{phase:guided.phase?.label||"Shift",openActions:Number(inbox.open||inbox.pending||guided.unacknowledged||0),focusScore:Number(guided.focusScore||0),profitSignal:profit.headline||null},operatorMode:typeof document!=="undefined"?document.documentElement.dataset.operatorMode==="true":false};}
+  setOperatorMode(enabled){if(typeof document==="undefined")return this.snapshot();const on=Boolean(enabled),centers=[...document.querySelectorAll('section[id$="Center"],section[id$="center"]')].filter(x=>x.id);if(on){this.previousVisibility.clear();centers.forEach(el=>{this.previousVisibility.set(el.id,{hidden:el.hidden,aria:el.getAttribute("aria-hidden"),inert:el.hasAttribute("inert")});if(!this.primaryCenters.has(el.id)){el.hidden=true;el.setAttribute("aria-hidden","true");el.setAttribute("inert","");}});document.documentElement.dataset.operatorMode="true";}else{centers.forEach(el=>{const prev=this.previousVisibility.get(el.id);if(!prev)return;el.hidden=prev.hidden;if(prev.aria==null)el.removeAttribute("aria-hidden");else el.setAttribute("aria-hidden",prev.aria);if(prev.inert)el.setAttribute("inert","");else el.removeAttribute("inert");});document.documentElement.dataset.operatorMode="false";this.previousVisibility.clear();}const snap=this.snapshot();this.eventBus?.emit?.("operator-service-flow:mode-changed",{enabled:on,snapshot:snap});return snap;}
+  refresh(reason="manual"){const snap=this.snapshot();snap.reason=reason;this.appState?.update?.({operatorServiceFlow:snap});this.eventBus?.emit?.("operator-service-flow:updated",structuredClone(snap));return snap;}
+  destroy(){this.off.forEach(fn=>fn?.());}
+}
+if(typeof module!=="undefined"&&module.exports)module.exports=BlueCurrentOperatorServiceFlowEngine;
+if(global)global.BlueCurrentOperatorServiceFlowEngine=BlueCurrentOperatorServiceFlowEngine;
+})(typeof window!=="undefined"?window:globalThis);--- a/client/js/modules/operatorServiceFlowCenter.js
+++ b/client/js/modules/operatorServiceFlowCenter.js
@@ -0,0 +1,6 @@
+(function(){"use strict";
+function createBlueCurrentOperatorServiceFlowCenterModule(eventBus,appState){const root=document.getElementById("operatorServiceFlowCenter");if(!root||!window.BlueCurrentOperatorServiceFlowEngine)return null;const e=new window.BlueCurrentOperatorServiceFlowEngine({eventBus,appState}),$=id=>document.getElementById(id),esc=v=>String(v??"").replace(/[&<>\"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'\"':"&quot;","'":"&#39;"}[c]));
+function render(s){$("osfScore").textContent=`${s.flowScore}%`;$("osfLive").textContent=`${s.liveStages}/6`;$("osfAttention").textContent=s.attentionStages;$("osfSurfaces").textContent=s.surfaceAudit.total;$("osfTechnical").textContent=s.surfaceAudit.technical;$("osfMode").textContent=s.operatorMode?"OPERATOR":"FULL";$("osfHeadline").textContent=s.attentionStages?`${s.attentionStages} service stage${s.attentionStages===1?"":"s"} need operator attention.`:"Service flow is consolidated into six operator stages.";$("osfStages").innerHTML=s.stages.map((x,i)=>`<article><small>${i+1}. ${esc(x.label)}</small><strong>${esc(x.status.toUpperCase())}</strong><span>${esc(x.question)}</span><p>${esc(x.summary)}</p><button type="button" data-osf-center="${esc(x.center)}">Open ${esc(x.label)}</button></article>`).join("");$("osfAudit").innerHTML=`<article><strong>${s.surfaceAudit.primary} primary operator surfaces</strong><span>${s.surfaceAudit.supporting} supporting/technical surfaces remain available outside Operator Mode.</span></article><article><strong>${s.operator.openActions} open actions · focus ${s.operator.focusScore}%</strong><span>${esc(s.operator.phase)}${s.operator.profitSignal?` · ${esc(s.operator.profitSignal)}`:""}</span></article>`;}
+function load(reason="manual"){render(e.refresh(reason));}
+root.addEventListener("click",ev=>{const id=ev.target.closest("[data-osf-center]")?.dataset.osfCenter;if(id){const el=document.getElementById(id);if(el){el.hidden=false;el.removeAttribute("inert");el.removeAttribute("aria-hidden");el.scrollIntoView({behavior:"smooth",block:"start"});}return;}if(ev.target.closest("#osfOperatorMode")){render(e.setOperatorMode(true));}if(ev.target.closest("#osfFullMode")){render(e.setOperatorMode(false));}});$("osfRefresh")?.addEventListener("click",()=>load("manual"));eventBus?.on?.("operator-service-flow:updated",render);load("startup");return{engine:e,load};}
+window.createBlueCurrentOperatorServiceFlowCenterModule=createBlueCurrentOperatorServiceFlowCenterModule;})();--- a/scripts/maintenance/test-operator-fine-comb.js
+++ b/scripts/maintenance/test-operator-fine-comb.js
@@ -0,0 +1,7 @@
+"use strict";
+const assert=require("assert");
+global.structuredClone=global.structuredClone||((x)=>JSON.parse(JSON.stringify(x)));
+const Engine=require("../../client/js/modules/operatorServiceFlowEngine");
+const state={guidedShift:{phase:{label:"Live service"},focusScore:72,unacknowledged:2},reservationYield:{headline:"Arrivals paced",waitlist:3,covers:86},operationalDigitalTwin:{occupancy:84,guestWait:9},occupancyPercent:84,kitchenThroughput:{headline:"Expo pressure",throughput:48,wait:14},laborDeployment:{headline:"One flexible role available",laborPercent:30},guestRecovery:{headline:"2 recovery moments require ownership",open:2},shiftProfitPulse:{headline:"Margin protected",margin:18.4,profitAtRisk:0}};
+const appState={getState:()=>state,update:()=>{},get:k=>state[k]};const eventBus={on:()=>()=>{},emit:()=>{}};
+const e=new Engine({eventBus,appState});const snap=e.snapshot();assert.equal(snap.stages.length,6);assert.equal(snap.stages.map(x=>x.id).join(","),"reservations,floor,kitchen,staffing,guest,profit");assert.equal(snap.attentionStages,2);assert.ok(snap.flowScore>=80);assert.equal(snap.operator.phase,"Live service");assert.equal(e.primaryCenters.has("operatorServiceFlowCenter"),true);assert.equal(e.primaryCenters.has("v45ClosureCenter"),false);console.log(JSON.stringify({ok:true,version:"46.5.0",stages:snap.stages.map(x=>({id:x.id,status:x.status})),flowScore:snap.flowScore,attentionStages:snap.attentionStages,operatorPhase:snap.operator.phase,operatorPrimarySurfaces:e.primaryCenters.size},null,2));--- a/README-APPLY-V46.5.0.md
+++ b/README-APPLY-V46.5.0.md
@@ -0,0 +1,65 @@
+# APPLY BLUE CURRENT V46.5.0
+
+## Baseline
+
+Apply over the authoritative repository containing V45.30.0.
+
+## Important
+
+This patch does **not** contain `database/data/blue-current.json`, database `.tmp`, or database `.bak` files.
+
+## Apply
+
+Extract `BLUE-CURRENT-V46.5.0-PATCH.zip` into the root of `blue-current-reservations`, preserving directories and replacing matching files.
+
+## Validate
+
+```powershell
+taskkill /F /IM node.exe
+npm run check
+node scripts/maintenance/test-operator-fine-comb.js
+npm run start
+```
+
+In another PowerShell window:
+
+```powershell
+curl.exe -s http://localhost:8787/api/health
+```
+
+Expected:
+
+```json
+"version": "46.5.0"
+```
+
+## Recommended Git staging
+
+```powershell
+git add package.json
+git add server/server.js
+git add server/api/router.js
+git add client/index.html
+git add client/js/app-v15.1.3.js
+git add client/js/startup-loader.js
+git add client/js/modules/operatorServiceFlowEngine.js
+git add client/js/modules/operatorServiceFlowCenter.js
+git add scripts/maintenance/test-operator-fine-comb.js
+git add README-APPLY-V46.5.0.md
+git add V46.5.0-RELEASE.md
+git add V46.5.0-IMPLEMENTATION.patch
+```
+
+Inspect before committing:
+
+```powershell
+git status
+git diff --cached
+```
+
+Commit example:
+
+```powershell
+git commit -m "V46.5.0 operator fine-comb service flow"
+git push origin live-service-timeline
+```
--- a/V46.5.0-RELEASE.md
+++ b/V46.5.0-RELEASE.md
@@ -0,0 +1,76 @@
+# BLUE CURRENT V46.5.0 — OPERATOR FINE-COMB I
+
+## Release scope
+
+V46.1–V46.5 begins the operator fine-comb phase after V45 architecture closure. This release does not add a new AI or autonomous execution stack. It consolidates existing restaurant-operating signals into a single operator workflow and adds a reversible Operator Mode for simplifying the interface before any permanent feature removal.
+
+### V46.1 — Six-Stage Operator Service Flow
+Adds one primary shift workflow:
+
+1. Reservations
+2. Floor
+3. Kitchen
+4. Staffing
+5. Guest Recovery
+6. Profit
+
+Each stage identifies the operating question, source center, current signal, summary, and attention state.
+
+### V46.2 — Cross-Surface Operator Snapshot
+The Operator Service Flow consumes existing AppState signals rather than duplicating domain engines. It reuses reservation yield/operations, floor/digital twin, kitchen throughput, labor deployment, guest recovery, Guided Shift, Action Inbox, and Profit Pulse state.
+
+### V46.3 — Surface Audit
+Adds a live inventory of platform centers and separates primary operator surfaces from supporting/technical surfaces. Technical/readiness/certification/integration surfaces remain available, but are no longer treated as part of the manager's primary shift path.
+
+### V46.4 — Reversible Operator Mode
+Adds an Operator Mode that hides non-primary centers without deleting or rewriting them. Returning to Full Platform restores prior visibility state. This lets Blue Current test a simplified operator experience safely before permanent module cuts.
+
+Primary operator surfaces currently include:
+- Unified Command
+- Guided Shift
+- Operator Service Flow
+- Operator Copilot
+- Command Action Inbox
+- Shift Profit Pulse
+- Operations Workspace
+- Reservation Operations
+- Live Floor Operations
+- Kitchen Throughput
+- Labor Deployment
+- Guest Recovery
+
+### V46.5 — Fine-Comb Validation
+Adds focused validation proving the six-stage sequence, attention-state behavior, operator context, and primary-surface classification.
+
+## Safety and architecture policy
+
+- No live execution behavior was added.
+- No V44/V45 governance or safety boundary was removed.
+- No working module was deleted.
+- Operator Mode changes presentation only; it does not mutate restaurant state or remove functionality.
+- The runtime database is not included in this patch.
+
+## Validation
+
+- `node scripts/maintenance/test-operator-fine-comb.js` passed.
+- Six operator stages validated in the required order.
+- Kitchen and Guest Recovery attention states were surfaced in the focused harness.
+- Operator flow score calculated correctly from live/attention state.
+- Primary versus supporting surface classification passed.
+- `npm run check` passed:
+  - 818 JavaScript files
+  - 700 client scripts
+  - 5,228 unique HTML IDs
+- Server startup passed as V46.5.0.
+- `/api/health` returned HTTP 200 with database connected and auth enabled.
+- Authoritative runtime database SHA-256 remained unchanged during startup validation.
+
+## Next fine-comb milestone
+
+The next wave should use this consolidated operator path to score and classify existing modules as:
+- keep primary
+- merge into primary
+- diagnostics/admin only
+- remove/deprecate candidate
+
+It should also tighten cross-stage handoff behavior and operator navigation before permanently deleting code.
