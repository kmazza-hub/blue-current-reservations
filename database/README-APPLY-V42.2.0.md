# Apply V42.2.0

Replace the included client and server files, add the new modules/service, then restart with `npm run start`.

Test at `http://localhost:8787/?pack=live`. Sign in first if prompted. Configure a sandbox connector, load the sample canonical event, ingest it, then refresh Live Source Health.
