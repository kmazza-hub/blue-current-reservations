# V100.3.2 — Human Operator Review Protocol

Requires V100.3.1. Extract into the root of `blue-current-reservations`, then run:

```powershell
node APPLY-V100.3.2.js
node scripts/maintenance/test-v100.3.2-human-operator-review-protocol.js
```

Use `V100.3.2-OPERATOR-REVIEW-WORKSHEET.md` with `config/operator-review-v100.3.2.json` during the hands-on review. This wave does not require a server restart because no runtime files change.
