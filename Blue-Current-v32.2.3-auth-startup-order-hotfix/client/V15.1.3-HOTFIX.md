# V15.1.3 Concierge Autoplay Hotfix

- Renamed the primary application script to `js/app-v15.1.3.js` so the browser cannot reuse an older `app.js` response.
- Removed all dependency on the click event object from the Concierge autoplay timer.
- Added a console build marker: `Blue Current build: V15.1.3`.
