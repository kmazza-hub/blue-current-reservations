# Apply V36.35.0

Replace the four existing client files and add the six new modules plus release documentation. Then run:

```powershell
git add .
git commit -m "V36.35 experience quality demo mode pilot onboarding"
git pull --rebase origin live-service-timeline
git push origin live-service-timeline
```
